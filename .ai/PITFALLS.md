# Pitfalls

## Risks

- Architecture breadth can consume the project before the state-aware memory loop works.
- LLM extraction can pollute memory and make tests nondeterministic if added too early.
- Multiple stores introduce consistency and deployment burden before the MVP proves value.
- A dashboard can hide weak runtime correctness.
- Benchmark claims will be weak unless vector-only and gated variants use identical seeded memory items.

## Likely Implementation Traps

- Attaching recovery nodes under failed nodes; recovery should attach to the failed node's parent.
- Sorting events only by `created_at`; use run-local `sequence_no`.
- Treating `branch_status` and lifecycle `status` as the same field.
- Letting failed/rolled-back memory enter prompt because semantic relevance is high.
- Making profiler writes part of the critical path.
- Physically deleting superseded or failed memories and losing provenance.
- Turning P0 regex memory writing into an unbounded NLP project.
- Adding Neo4j/ES/Celery before PostgreSQL-only correctness is proven.

## P0 Implementation Findings (encountered & handled)

- **Superseded/archived memory leaking into prompt.** Filtering only `deleted` at the candidate stage let user-corrected constraints (status `superseded`) survive and get injected when context-merge order happened to favor them. Fixed by an explicit `_RETRIEVABLE_STATUSES` allowlist (`active/pinned/conflicted/quarantined`) in the retrieval controller. Lesson: lifecycle `status` must be filtered at retrieval, not only relied on at merge time.
- **branch_status vs lifecycle status are two independent gates.** A memory can be `branch_status=completed` yet `status=superseded` (and vice versa). Both must be checked: branch validity in the gate, lifecycle validity at candidate selection. Confusing them silently injects stale data.
- **Single-filter retrieval path is fragile.** Lifecycle filtering currently lives only in the candidate stage. If a second retrieval path is added (e.g. pgvector KNN in P1), it must reapply the same `_RETRIEVABLE_STATUSES` filter or stale memory reappears.
- **Benchmark fairness depends on workspace isolation in the demo.** Sharing one workspace across strategies on the SQL backend accumulated duplicate candidates across runs and skewed counts. Fixed by per-strategy unique workspaces; keep seeded memory sets identical and isolated.
- **Profiler latency can read 0ms.** Sub-millisecond phases round to 0 in reports; this is expected for the in-memory path and not a measurement bug.

## P1 Implementation Findings (encountered & handled)

- **Benchmark case definitions are not enough without a runner.** `cases.py` and `evaluator.py` seed/evaluate data, but MVP requires actual JSON/Markdown report artifacts. Add/keep a runner that executes every case x strategy and writes `benchmark_report.md` + `benchmark_results.json`.
- **Benchmark reports are not the same as benchmark persistence.** If a reviewer asks for Task 14 persistence, ensure the runner can write `benchmark_cases` + `benchmark_results` via the repository; report files alone are not enough.
- **Generated benchmark reports are ignored.** `reports/` is intentionally ignored; when checking P1 completeness, run `python -m app.benchmark.runner --output-dir reports` rather than looking for tracked report files.
- **Active-path summaries must exclude failed progress.** The `active_path` context block should be built from nodes accepted by `active_path_node_ids`; otherwise rolled-back failed summaries can re-enter prompts as state context even when memory gate rejects failed-branch memories.
- **Basic dashboard tables should stay table-shaped.** P1 needs inspectable rows, not a full dashboard app. Keep `/v1/dashboard/tables` focused on runs/access/profile/benchmark rows and avoid React/UI scope creep before P2.
- **variant_1 sharing baseline_1's contamination rate is correct, not a bug.** mvp.md §10.1 defines variant_1 as state-aware rerank that only *downweights* failed branches (no hard reject). A downweighted failed memory can still pass top-k into context, so variant_1 may show the same `failed_branch_contamination_rate` as baseline_1; only variant_2 (hard+risk gate) drives it to 0. Do not "fix" variant_1 to reject — that would collapse it into variant_2.
- **cross_workspace_leakage is 0 for every strategy by construction.** Candidate retrieval is workspace-scoped, so even baseline_1 cannot pull another workspace's memory. This satisfies the §10.5 security invariant but means case_3 cannot show a baseline-vs-variant *quality* gap; it proves the permission filter, not a ranking improvement.
- **Encode §10.5 pass criteria as a runner self-check.** Metrics alone don't assert acceptance. The runner emits an `acceptance` block (criteria 1-3) asserted by a unit test; criteria 4-6 (project constraints, profiler fields, access inspection) are covered by existing unit tests, not the benchmark.
- **Keep `__init__.py` in every test package.** New `tests/benchmark/` and `tests/api/` initially lacked `__init__.py` while sibling packages had them; add them to match collection behavior.

## Environment Pitfalls

- **pgvector restored (2026-06-09).** The `pgvector/pgvector:pg16` image is now available, so semantic retrieval is back: `embedding_vector` is `vector(256)` with an HNSW cosine index. Pitfalls learned: (1) PG15 data volumes are incompatible with the pg16 image — switching requires `docker-compose down -v` (destructive, recreates the volume); (2) this env has standalone `docker-compose` only (the `docker compose` subcommand errors out); (3) alembic must run from the repo root (where `alembic.ini` lives) with `PYTHONPATH=apps/api` so `app.*` imports resolve; (4) embeddings MUST use a process-stable hash (blake2b), not Python's salted built-in `hash`, or persisted vectors won't match query vectors across processes.

## Over-Engineering Warnings

- Defer multimodal ingestion, full knowledge graph, complex reflection, trained gate, and enterprise governance.
- Prefer JSON/Markdown report over full dashboard until gate/state workflow is stable.
- Keep P0 memory types and rules small enough to test exhaustively.
- Do not implement generic RAG features unless they support agent runtime behavior.

## Testing Pitfalls

- Testing only happy-path retrieval; include failed branch, stale, secret, workspace mismatch, and tool-sensitive cases.
- Using LLM generation as the only evaluator; also test context pollution before generation.
- Forgetting negative project constraints (`should not use Node.js`) when packing positive constraints (`uses Bun`).
- Not verifying gate logs for every candidate, including rejections.
- Not checking profiler does not block or fail the main request.
- Context compaction tests must cover protected-block budget pressure: if a protected block consumes the full budget while ordinary blocks are dropped, the packer must still make room for a real `compaction_notice` block (not merely set `PackResult.notice`). Also verify config knobs are wired through every pack_context callsite (hot path, inspect, replay), not only declared in `Settings`.
- Durable compaction logs must be written only after `MemoryAccessLog.access_id` exists. Keep the `PendingCompactionLog -> RetrievalPipelineTrace.pending_compaction_logs -> RetrievalController._persist_trace -> ContextCompactionLog` path single-sourced; do not add second write sites in packer, runtime, replay, or reports.
- C3 summarizer provider validation is intentionally conservative: LLM retained facts must be drawn from structured `must_retain_facts` and must cover every required full identity (`key/value/source_memory_id/provenance.run_id/provenance.step_id/provenance.event_id/provenance.state_node_id`), not only key/value. Do not parse free-form block text as allowed facts because negated/stale/risky text can contain `key=value` strings. Recompute `post_tokens` locally (never trust LLM token counts), require top-level source id sets to be preserved, reject invented or misbound source ids/provenance, and make any HTTP/JSON/schema/validation failure fall back to `RuleSummarizerProvider` with `provider=fallback_rule`. When sorting/deduping retained facts, normalize nullable provenance fields for ordering; Python cannot compare `None` and `str`. C4 replay now reads persisted `history_summary` compaction logs and must never rerun the provider. C4 has two filter sets: raw active-path events skip failed/rolled_back nodes, redacted secrets, and risky tool results; retained `MemoryItem` facts still require retrievable lifecycle status, active branch, non-stale, non-secret, non-risky provenance.
- Failure-aware negative memory must not reuse the positive accepted-memory path. `degrade` was historically included in `GateOutcome.accepted` and in independent `_ACCEPTED_DECISIONS` sets in metrics/replay/inspect; when enabling failure learning, all copies must change to `accept/warn` only or degraded failed memories will be packed, counted, or replayed as positive context. Unsafe failed memories can be rejected before risk-policy reasons run, so safe/unsafe splitting must explicitly inspect sensitivity/risk flags inside the failed/rolled_back hard-policy branch rather than relying on `reject_reason`. Use the shared `retrieval/negative_evidence.py` builder for controller/inspect/replay; do not let packer consume raw unsafe `MemoryItem.content`. The builder must remain defensive: even if replay/history/drift input marks an unsafe memory as `degrade`, it must re-check memory risk flags and emit a sanitized notice instead of raw failed-attempt text. Inspect/replay candidate and gate views must also use a safe renderer; fixing context blocks alone is insufficient because observability APIs can leak raw unsafe failed content through candidate payloads. Controller warnings/profile must count retained `avoided_attempts` blocks, not merely constructed `NegativeEvidence` objects, because negative evidence is ordinary context and may be dropped under budget pressure.
- Failure-aware safe rendering must treat explicit metadata as authoritative, not rely only on regex redaction or failed-branch status. `RiskFlags.contains_secret` should hard-reject as `secret` even for completed/active memories; `safe_observability_content(...)` must template on `reject_reason="secret"`, `Sensitivity.secret`, or `risk_flags.contains_secret` before falling back to regex; replay candidate `key/value` fields need the same hiding policy as candidate `content` because raw commands/secrets can live outside `content`.
- Failure-aware benchmark metrics must distinguish **positive contamination** from the user's current task/query wording. `active_state` may legitimately contain phrases like "npm failure" because the current user asks about a prior failed attempt; that is not failed-branch memory contamination. Score positive contamination via the existing `contaminated()` predicate over positive blocks (failed npm evidence), while scoring `negative_lesson_retained` / `unsafe_negative_leakage` only over `avoided_attempts` / `source=negative_evidence` blocks. Do not mark any positive-block occurrence of a marker such as `npm` as contamination, or case_10 will false-fail when the current goal mentions the avoided command.
- When benchmark acceptance grows, update or qualify **all** historical verification snippets that quote the acceptance count. Context Compaction C5 originally ended at `8/8`, but Failure-aware I5 expands the global suite to `10/10`; future docs should say whether a count is historical-to-that-slice or current-global to avoid confusing reproducibility checks.
- When adding a second Python package with its own `tests/__init__.py` (for example `packages/python-sdk/tests` next to `apps/api/tests`), pytest's default import mode can collide on the top-level `tests.*` package name during full-suite collection even if targeted package tests pass. Use `--import-mode=importlib` in root pytest config before declaring the slice complete, and always run the full root suite after adding new test roots.
- SDK backend isomorphism tests must exercise the **same runtime instance**, not two independent in-memory runtimes that merely produce similar shapes. For HTTP backend tests with `httpx.ASGITransport`, override FastAPI `get_runtime` to the exact `MemoryRuntime` used by `MemTrace.in_process(...)`, then cross-read/write (`in_process` writes, HTTP reads, and vice versa). Also cover all list-shaped HTTP responses (`timeline`, `state-tree`, `steps`, `profile`, `memories`), because `TypeAdapter(list[Model])` parsing can drift per endpoint even when one list endpoint passes. If an S2-style backend contract says it mirrors every `/v1` endpoint, include singular read helpers too (for example `/v1/steps/{step_id}` → SDK `get_step`) or explicitly narrow the plan; otherwise docs can overclaim endpoint coverage while tests pass on the headline path.
- LangGraph adapter hooks should remain framework-light until a true graph-compilation helper exists: `before_node` / `after_node` / `on_error` can operate only on the SDK `MemTrace` facade and must not import `langgraph` at module import time. Adapter-written events must pass `event_source="langgraph_adapter"` explicitly, because the generic SDK facade defaults omitted sources to `"sdk"`. Failure-path tests must preserve I3 semantics: failed branch content is excluded from positive context, but safe lessons may appear as `avoided_attempts` with `source="negative_evidence"`; blanket assertions that failed text is absent from all context blocks would false-fail valid failure learning.
- CLI operational commands must not default to `MemTrace.in_memory(...)`: every shell invocation is a new process, so a fresh `InMemoryRepository` cannot see a run created by a previous command. Require `--http` for stateful commands and reserve in-process mode for one-shot `demo`. Also stamp CLI writes with `event_source="cli"`; otherwise the SDK facade's default `"sdk"` label hides which entrypoint generated the event.
- SDK/HTTP isomorphism must account for path-sensitive identifiers. `StartRunRequest.session_id` is an arbitrary string, so `HttpBackend.flush_session(...)` must use body JSON (`POST /v1/sessions/flush` with `FlushRequest`) rather than interpolating `session_id` into `/v1/sessions/{session_id}/flush`; otherwise values such as `tenant/session` work in-process but 404 over HTTP.
- Phase-closeout docs must update README, the task plan, ROADMAP appendix, AGENTS, PROJECT_STATE, REQUIREMENTS, IMPLEMENTATION_PLAN, and OPEN_QUESTIONS together. Leaving any one of those pointing at the just-completed slice (for example S6) makes resume/project-state recovery choose stale work. Also avoid marking a whole slice as ready to move to the next roadmap item while its explicit closeout task (for example §7 Task 11 full regression/reproducibility/project-memory sync) is still pending.
- Six-strategy rollout is intentionally incremental: adding `RetrievalStrategy.long_context` / `variant_3` in Task 1 only makes the values parseable; Task 2 only wires their gate config contracts (`long_context` all-policies-off, `variant_3` full gate plus `enable_reflection_rerank`); Task 3 wires actual `long_context` include-all/unbounded-budget controller behavior while preserving workspace/lifecycle filters and normal gate/log persistence; Task 4a isolates benchmark `access_count` by snapshotting seed-time counts and restoring before each strategy retrieval so `variant_3` does not observe earlier-strategy side effects; Task 4b wires actual `variant_3` deterministic reflection-lite accepted-memory rerank; Task 5 expands `ALL_STRATEGIES` to the ROADMAP §7 sequence `baseline_0 -> long_context -> baseline_1 -> variant_1 -> variant_2 -> variant_3`; Task 6 adds `case_12_reflection_retention` and moves counts to 12 cases × 6 strategies = 72 results/accesses with 14 seeded runs; Task 7 surfaces `reflection_retention_hit_rate` and adds `variant_3_retains_high_value_memory_under_budget` / `long_context_shows_token_bloat` acceptance; Task 8 persists benchmark runs into `eval_cases` / `eval_runs` / `eval_results` without a migration; Task 9 mirrors `reflection_retention_hit_rate` into the dashboard benchmark summary; Task 10 closes docs by marking ROADMAP §7 complete, documenting that deterministic reflection-lite must be superseded by the real §3.2 Reflection/Forgetting scheduler, and updating README to list all six strategies plus the full benchmark case family; Task 11 must verify not only the single-shot benchmark but also repeatable `run_benchmark(..., repo=same_repo)` semantics. Keep the rerank scoped to accepted positive memories before packing, and remember the packer still owns protected/project/type ordering; same-type episodic fixtures are the stable way to prove retention contrast. The case_12 budget must be tuned around protected active-state/path plus compaction notice: `token_budget=10` is too small and drops all ordinary episodic blocks, while `token_budget=32` leaves enough ordinary budget for `variant_3` to retain `RETAIN-CRITICAL-FACT` and `variant_2` to miss it. Enum/benchmark tests should guard exact membership/order because downstream benchmark/report code assumes that sequence. Acceptance checks must be present-row/comparator guarded: `reflection_retention_hit_rate` alone is insufficient without explicit `case_12` present rows, and `long_context_shows_token_bloat` must require both `long_context` and a `variant_2` comparator rather than defaulting a missing comparator to `0.0`. Eval persistence must compute summary/acceptance before writing the eval run so `EvalRunRecord.config` captures the same verdict as the JSON report; `EvalResultRecord.passed=True` means the row executed, not that `metrics["task_success"] == 1`; repeatability should upsert stable case ids but append a fresh eval run plus 72 eval results per benchmark invocation, and each persisted benchmark invocation must use an isolated workspace prefix so historical memories in the same repo cannot pollute the next run's candidate set/compaction/token-overhead metrics. Long-context must not use a fixed large sentinel as a fake infinity: if the requested budget would truncate or drop blocks, expand to the exact `PackResult.pre_compaction_tokens` and persist that effective budget so replay/reporting remain truthful. For `variant_3`, persist the reflection-lite rerank score into the gate log `final_score` before writing logs; replay original-view reconstruction should sort by persisted scores, not recompute retention from current `MemoryItem` state after access-count bumps or later trust/freshness changes. Repeatability assertions should exclude timing-only latency fields (`avg_retrieval_latency_ms`, `avg_gate_latency_ms`) because wall-clock profiler values are intentionally observational, not deterministic benchmark semantics. Dashboard summary parity tests should compare present-flag-gated benchmark metrics against `run_benchmark(...)` output whenever adding a new summary field. Public docs review should update both the strategy list and the benchmark case coverage sentence; otherwise README can correctly list six strategies while still under-reporting `case_10`/`case_11`/`case_12`.
- Security & Consistency Hardening H1-H18 is complete per `docs/design/SECURITY_CONSISTENCY_HARDENING_PLAN.md`, §10 Provider Registry / §11 Key Ontology is complete per `docs/design/PROVIDER_REGISTRY_KEY_ONTOLOGY_PLAN.md`, I7 compaction-negative retained facts is complete through I7.6, and Phase 4 P4-A/P4-B are complete. Preserve the durable H1-H18 contracts when extending the system: positive context redaction must be packer-level defense-in-depth, not only candidate filtering; `variant_1` should relax failed/rolled_back rejection without disabling secret/destructive/quarantined policies; auth must remain default-off for local/dev/benchmark; event sequence allocation and insert must be one SQL transaction; timeout access ids must be inspectable and not split from partially persisted trace logs; policy snapshots must hash stable non-secret config only; conformance tests should encode durable invariants rather than brittle implementation details.
- After H1-H3, remember that safety-floor semantics are intentionally layered: `baseline_1` and `long_context` may still disable portions of hard/risk policy for ablation, but they must not bypass quarantined, explicit secret/contains-secret, destructive-command, or tool-sensitive safety floors. Packer-level redaction remains required as defense-in-depth for unflagged/future bypass paths, including non-prompt metadata such as `PackResult.retained_constraints`, pending compaction logs, profile metadata, replay, and reports; tests for explicitly flagged unsafe memories should expect gate rejection rather than a redacted positive block. Failure-learning sanitized reject reasons should still win for unsafe failed/rolled_back branches, so place generic safety floors after failed-branch sanitization logic.
- After H4/H5/H6/H13, keep error and persistence boundaries single-sourced: replay run-existence checks belong in `MemoryRuntime`, with routes/SDKs only translating errors; event hot-path writes must use `Repository.append_event(...)`, not separate `next_sequence_no(...)` + `add_event(...)`; retrieval timeout should wrap `trace(...)` construction only, and both timeout branches must persist an inspectable minimal `MemoryAccessLog`; corrupted state-node references should raise `StateTreeError` before status writes, buffered flush, or other side effects rather than returning ghost nodes or step-only rollback results. Preserve HTTP replay missing-run detail as `"run not found"` for existing API compatibility while SDKs expose `NotFoundError`. Keep event unique constraint naming aligned with the existing initial migration (`uq_event_run_seq`) and avoid duplicate same-column constraints in later migrations.
- After H7-H10, keep determinism/policy/conformance contracts aligned across hot path and replay: gate-log row iteration is `(created_at, gate_id)`, but accepted-context reconstruction is `(-final_score, memory_id)`; do not conflate those two orderings. Whenever retrieval policy semantics change (gate config, token budget expansion, vector/include_all, lifecycle filter version, packer reserve, provider determinism), update `retrieval/policy.py` and expect replay to emit `policy_drift` for old hashes; old rows with no hash should remain `policy_snapshot_missing` warnings, not critical drift. Any new retrieval entrypoint/provider/scheduler/adapter should add or satisfy `apps/api/tests/conformance/` rather than relying on scattered existing tests.
- After H11/H12/H14, remember token budgeting is now intentionally independent from retrieval tokenization: do not reuse stopword-dropping similarity tokens for prompt budgets, and keep CJK/no-whitespace/mixed truncation budget-closed. Structured `key=value` facts must stay compact enough for `compacted_constraints`; if budget precision changes, verify `case_9_over_budget_compaction` semantics and update `retrieval/policy.py`'s `TOKEN_ESTIMATOR_VERSION` rather than blindly updating numeric snapshots. Summarizer validation should trust only structured `must_retain_facts` for retained-fact/provenance allow-sets, while still requiring top-level source-id preservation; do not add fact-local `source_memory_id` values to the top-level allowed memory set unless they already appear in `request.source_memory_ids`. Benchmark fairness snapshots must restore whole `MemoryItem` rows before each strategy and fail on both new memories and missing/moved snapshot memories; future reflection/scheduler mutations must not leak into later strategy comparisons. Packer redaction must cover retained-fact keys as well as values, because secret-like material can appear in controlled keys before §11 ontology exists; auth comparisons should operate on bytes so malformed/non-ASCII credentials fail closed as 403 rather than raising `TypeError`.
- After H15-H18, migration policy tests should check declarations and compatibility rules without requiring live PostgreSQL by default; keep the real `alembic upgrade head` path guarded by `MEMTRACE_TEST_DATABASE_URL` so local/CI runs remain deterministic. Trace bundle export must stay redacted-by-default and validation-only: do not add production write-import without explicit workspace/id-collision/redaction-trust design. Redaction needs to walk nested metadata/policy snapshots/retained facts as well as top-level event/memory strings. Dogfood scripts should remain no-network, short-output, and subprocess-smoke-testable; for destructive failure demos, assert raw commands are absent from stdout and use query terms that actually retrieve the unsafe failed memory so sanitized negative evidence is exercised rather than accidentally omitted.
- Provider Registry / Key Ontology plan traps (2026-06-13): keep `providers.registry` lightweight and dependency-free from settings/provider implementations; put settings-based construction in `providers.factory`. Capability and policy snapshots must recursively freeze/sanitize metadata and must not contain API keys, bearer tokens, auth headers, raw request bodies, raw provider responses, or secret-like string values. P1/P2/P9 review already found and fixed one value-side sanitizer gap: do not only filter secret-like metadata keys or `sk-`/`bearer` values; values containing token/password/secret/credential/private-key markers must also be dropped. Benchmark code must explicitly use `deterministic_provider_registry()` rather than `build_provider_registry(get_settings())`, or real-provider env vars can break reproducibility. Retrieval policy snapshots should include only retrieval-relevant providers (`embedding`, `summarizer`) and exclude contract-only `judge`, or non-retrieval registry changes will create false policy drift. `Repository.add_memory(...)` must keep deterministic embedding fallback for direct seeded memories/tests/backfills even after runtime uses an embedding provider. Runtime write paths should call `_prepare_embedding(...)` before every internal `repo.add_memory(...)`, but provider failure must not block writes and must fall back to deterministic `stable_embedding(..., 256)`; retrieval query vectors should use `_embed_query(...)` with the same degradation. Provider-returned vectors must be exactly 256-dimensional and finite before reaching SQL pgvector or vector search; wrong dimensions, NaN, and +/-inf should be treated as provider failure because this slice does not migrate the pgvector column and vector math can silently degrade. If tests need malformed JSON numeric values such as NaN, return raw response content through `httpx.MockTransport` rather than `json=...`, because httpx serializes response JSON strictly. `RetrievalController.provider_snapshot` should be frozen with the cached embedding provider lifecycle so later mutations to a shared registry cannot make access policy snapshots describe a different provider than the one used for query embedding. Explicit runtime provider overrides (notably `summarizer_provider=`) must be reflected in the policy snapshot rather than blindly using the registry slot, or replay/policy hash can describe a provider different from the one used by runtime compaction. Replay policy-drift reconstruction should use public `RetrievalController.provider_snapshot`, not a private snapshot helper, so it stays source-aligned with hot-path policy hashing. Key ontology must be the only source for writer constants, resolver cardinality, LLM prompt text, aliases, and free-form validation; otherwise drift returns. Concrete free-form keys must reject `*`, and wildcard specs should only provide inherited defaults for safe children. Runtime active-memory identity and supersede matching must compare canonical identities (e.g. historical `project.pkg_manager` vs `project.package_manager`), not raw keys. `AccessInspection` is a flat DTO with policy fields, and `StartStepRequest` uses `intent`, not a `name` field.
- P5-P7 ontology migration findings (2026-06-13): `key_ontology.normalize_memory_key(...)` intentionally returns a warning rather than raising so LLM extraction can drop unsafe/unknown candidates without failing the whole provider batch. Keep provider parsing (`LLMExtractionProvider.extract`) schema-tolerant and do ontology enforcement in `build_results(...)`, otherwise runtime fallback/error semantics change. Historical alias compatibility must be enforced at the runtime identity boundary too, not only in resolver cardinality, because resolver only sees memories prefiltered by `MemoryRuntime._same_identity_actives(...)`. Prompt rendering must use `MemoryScope`'s real enum values (`workspace/session/user`), not stale `run/global` wording.
- P5-P7 post-review fixes (2026-06-13): once `project.package_manager` exists, deterministic writer rules must not keep treating `npm/pnpm/yarn` as `project.runtime`, or package-manager updates will supersede runtime facts. Alias-aware dedup needs both matching and canonical survivor promotion; otherwise same-value `project.pkg_manager` rows remain active under historical aliases forever. Safe arbitrary free-form keys such as `project.framework` may not have a wildcard spec, so `build_results(...)` must still force durable `project/workspace` defaults instead of trusting provider-supplied `episodic/session`.
- P8/P10 provider benchmark isolation findings (2026-06-13): benchmark runtime construction must explicitly pass `deterministic_provider_registry()` rather than relying on `MemoryRuntime` defaults or settings-derived registries; otherwise real `MEMTRACE_EMBEDDING_PROVIDER=openai` / LLM summarizer env vars can alter policy snapshots, attempt network calls, or break reproducibility. Provider conformance should assert retrieval policy snapshots contain only retrieval-relevant `embedding`/`summarizer` metadata, exclude `judge`, and do not render `api_key`/`authorization`/`sk-`/`secret` strings. Keep repository-level deterministic `ensure_embedding(...)` fallback for direct seeded memories even when benchmark/runtime call sites use provider registries. Settings-derived embedding providers must stay pinned to the fixed 256-dim pgvector contract even if `MEMTRACE_EMBEDDING_DIM` is set differently; otherwise snapshots describe an unusable provider while runtime silently falls back.
- Provider/key ontology final-review findings (2026-06-13): dual-use tokens require correction-context semantics. `bun` is both runtime and package-manager-like in project language; corrections such as `不是 npm，是 bun` / `不用 npm，用 bun` must preserve the old key family and supersede `project.package_manager`, not write `project.runtime=bun`. Factory tests should cover summarizer real/degraded wiring as well as extraction/embedding/judge, and ontology tests should lock the declared controlled schema including non-LLM-extractable keys such as `tool.command.failed`.
- I7 compaction-negative retention plan traps (2026-06-13): retained negative evidence is metadata only; never make `avoided_attempts` protected or force it into prompt context under budget pressure. Do not overload positive `retained_facts`; use dedicated `RetainedNegativeEvidence` and `ContextCompactionLog.retained_negative_evidence`. PostgreSQL JSONB defaults should use `sa.text("'[]'::jsonb")`; SQL mappings should write `model_dump(mode="json")` and read `RetainedNegativeEvidence.model_validate(...)`, with old `None` rows mapping to `[]`; Alembic `down_revision` must be the actual current head revision id, not just a guessed filename prefix. Keep `risk_kind` tolerant (`str | None`) or normalize aliases so new reject/risk reasons do not crash replay/reporting. Packer retention should match only the standard negative block shape (`type="avoided_attempts"` AND `source="negative_evidence"`) and dedupe by source ids + mode + reason + safe_text, not by safe_text alone. Do not look up retained negative evidence through `memory_by_id`, because that map is accepted-positive-memory-only; use `negative_by_memory_id` / `negative_by_state_reason` and preserve `source_state_node_id` through fallback `ContextBlock.provenance` when `NegativeEvidence.provenance` is absent. Replay should not infer I7-era from retrieval policy snapshots because I7 changes compaction metadata, not retrieval semantics. Benchmark case 13 must prove metadata retention/safety without implying retained metadata entered prompt; scan generated reports/bundles for unsafe markers (`rm -rf`, `/prod`, `sk-`, `password`, `Authorization`).
- I7.1/I7.2 implementation findings (2026-06-13): when adding a required field to frozen `PendingCompactionLog`, update every constructor immediately (`packer`, rolling history compaction, replay history prelude) or unrelated compaction paths will crash before I7.3 runs. Trace bundle redaction must not merely regex-redact retained negative `safe_text`: if a malformed/historical `sanitized_risk_notice` row carries raw destructive wording, export should fall back to the fixed `SANITIZED_TEMPLATES[risk_kind]` template. Migration tests should assert both ORM metadata server default and migration source, because untracked new Alembic files are easy to miss in ordinary `git diff HEAD` review.
- I7.3/I7.4 implementation findings (2026-06-13): retained negative evidence must be extracted from dropped `avoided_attempts` blocks by mapping back to safe `NegativeEvidence` DTOs, never by parsing prompt text or reading raw failed `MemoryItem.content`. If `NegativeEvidence.provenance` is absent, preserve `source_state_node_id` via block fallback provenance before conversion so replay/report metadata still identifies the failed branch. Metrics must keep `negative_evidence_block_count` as actual prompt blocks only; retained metadata needs separate `retained_negative_evidence_count` / `sanitized_retained_negative_evidence_count` counters in access metrics, summary, replay, and reports.
- I7.5/I7.6 benchmark closeout findings (2026-06-14): benchmark case 13 must score retained negative evidence from `ContextCompactionLog.retained_negative_evidence`, not from final prompt blocks, because retained metadata is not prompt input. Acceptance must require case-specific present flags (`retained_negative_evidence_count_present`, `compaction_negative_lesson_retained_present`, `compaction_retained_negative_unsafe_leakage_present`, `positive_contamination_present`, `correct_action_present`) so default zeros cannot make the check pass on missing rows. Markdown, JSON, persisted benchmark records, eval records, and dashboard `_benchmark_summary_from_records(...)` must all add new metrics together; otherwise report/dashboard parity breaks even when the runner summary is correct. Case 13 `task_success` only proves the existing positive/project context path still succeeds under compaction; never describe it as evidence that retained negative metadata entered the prompt. When adding benchmark cases, update dashboard run/access/case/result counts and reproducibility acceptance counts in all tests/docs.
- Project-memory closeout trap (2026-06-14): after marking a slice complete, search not only for explicit “next task” wording but also for section headers and summary phrases like “Current selected implementation target”, “selected plan”, or “current implementation target”. These can keep resume/project-state recovery pointed at the just-finished slice even when the Latest Verification section is correct.
- Phase 4 P4-A1/P4-A2/P4-A3 review findings (2026-06-14): task contract safety must reject raw secret-like **values** anywhere in payload/result metadata/error, not only secret-like keys or `redacted_event` values. Production async idempotency must be settings-driven: once async tasks are enabled with Redis, the default worker path cannot keep using process-local `InMemoryIdempotencyStore` or multi-worker dedupe fails. Redis candidate-buffer keys and set members must encode workspace/session ids instead of raw `:`-joined segments; otherwise ids with separators collide and secret-like session ids leak through keys/slowlog. Real Redis drain needs an atomic detach/rename (Lua or equivalent) before index removal; `LPOP` loops alone still have an empty-list/concurrent-append/index-delete race. Buffered flush paths and async worker extraction must share a safe persisted-event entrypoint that rechecks run/step/state terminal failure, rollback, and cancellation; `finish_step`/`complete_run` should persist failed/cancelled status before lazy flush so failed events do not become completed active memories. Event metadata must be recursively redacted before persistence/buffering, because content redaction alone does not prevent raw request payloads or credentials from entering Redis values.
- Phase 4 P4-A4/P4-A5 findings (2026-06-14): never add an enum member literally named `async`; use `ExtractionMode.async_ = "async"` so JSON stays natural while Python remains valid. Keep `buffered` observable behavior backward-compatible (`WriteEventResult.buffered=True`) even if internals share lazy buffering. `sync_flush` means explicit flush only; do not let retrieve/finish/complete window-boundary flushes process it automatically. Async enqueue payloads must stay event-id-only plus context ids (`event_id/run_id/workspace_id/session_id`), not serialized `AgentEvent` content. If enqueue fails after event persistence, fall back to the post-redaction lazy buffer with a stable warning rather than losing extraction work or running inline unexpectedly. P4-A5 dev compose must not mutate the core PostgreSQL-only `docker-compose.yml`; Redis smoke must remain `MEMTRACE_TEST_REDIS_URL`-gated and skipped in default full pytest.
- P4-A4/P4-A5 final-review findings (2026-06-14): request-level extraction mode overrides must be honored independently from the runtime default. A default `sync` runtime plus `WriteEventRequest.extraction_mode=buffered/lazy` must still flush at natural retrieve/finish/complete boundaries, while request-level `sync_flush` must remain explicit-only even under a default lazy runtime; keep lazy/window and explicit-flush buffers separate rather than deciding solely from `self._extraction_mode`. When merging Redis primary and local fallback drains, dedupe by `event_id` so partial Redis append/fallback ambiguity cannot double-extract the same persisted event. Real Redis append should be atomic (`RPUSH` + indexes in one Lua script), and drain failure after detach must restore indexes and popped values so candidates remain discoverable for a retry.
- P4-B1/P4-B4 lifecycle/reflection findings (2026-06-14): keep scheduler-derived `retention_score` / `reflection_priority` in `memory_retention_signals`, not on `memory_items`, so P4-C versioning can ignore scoring/access-only mutations. `variant_3` must bulk-load retention signals for accepted candidate ids and fall back per-memory to deterministic reflection-lite only when signals are absent; do not let a signal make an otherwise non-relevant/non-retrievable memory become a candidate. Retrieval policy hashes must include `reflection_signal_source`, `retention_policy_version`, and signal/fallback coverage because mixed scheduler/fallback ranking changes semantics. Access bump must update `last_accessed_at` with `access_count`, but this is still access-only and must not create P4-C memory versions. Lifecycle transitions should go through `transition_memory_status(...)` and write audit rows; repository writes must reject stale `audit.from_status` before updating/writing audit so scheduler cannot overwrite a concurrently pinned memory; scheduler should skip stale transitions. Retention scoring and fallback-lite scoring must treat NaN/+Inf/-Inf as safe low scores, not clamp them into high priority. Replay policy drift should compare against the current side-effect-free trace's policy snapshot/hash rather than hand-reconstructing scheduler signal source from the historical accepted set. Unpin should restore only safe previous statuses. Replay reconstruction creates a `RetrievalRequest`, whose strategy field is `strategy`, not `retrieval_strategy`.
- P4-C memory versions/conflicts findings (2026-06-14): version snapshots are audit metadata and must use `redacted_memory_snapshot(...)` exclusively; do not call `MemoryItem.model_dump()` directly from version writers because content/summary/key/value/lifecycle metadata can contain raw secret-like material. Keep embedding vectors out of version snapshots. Redaction must combine field-name/text regex with memory-key semantics: if `MemoryItem.key` is secret-like (for example `project.password` / `project.api_key`), redact the paired `value` even when the value is a short credential that does not match secret regexes. Version creation should track semantic changes (`content/summary/key/value/scope/status/risk/sensitivity/lifecycle_metadata`) and lifecycle transitions, but must ignore access-only `access_count`, `last_accessed_at`, and scheduler/access `updated_at` bumps or retrieval will create noisy version history. When `update_memory(...)` receives a stale semantic-update copy after a concurrent access bump, preserve the current row's access-only fields rather than rolling them back; benchmark full-snapshot restore remains an intentional access-only update and should still be able to restore access counts. `retrieve_context(...)` must treat `run.workspace_id` as authoritative and reject explicit `RetrievalRequest.workspace_id` mismatches before retrieval/history folding, or callers can use a valid run id to query another workspace. Conflict scan must reuse key ontology helpers (`canonical_memory_key`, `same_memory_key_identity`, `is_single_valued_key`) so historical aliases such as `project.pkg_manager` and canonical `project.package_manager` stay aligned with writer/resolver semantics. P4-C read APIs are intentionally read-only and workspace-scoped; `GET /v1/memory-conflicts` requires `workspace_id` and admin/global conflict views belong to P4-D governance, not P4-C.
- P4-D governance findings (2026-06-14): keep governance default-off (`governance_enabled=False`, `auth_enabled=False`, `quota_enabled=False`) so local/dev/benchmark/reproduce remain deterministic. API keys must store only lookup prefix plus digest; once DB keys exist, legacy `MEMTRACE_API_KEY` fallback must be disabled unless explicitly allowed, or hosted deployments silently retain a global admin token. Route-level authorization needs resource-id ownership lookup for run/step/access/memory/eval ids before returning data; missing resources stay 404, existing unauthorized resources stay 403, but invalid same-run workspace override remains a bad request (HTTP 400 / SDK `BadRequestError`) rather than an auth denial. Anonymous/default-off list endpoints historically return empty lists for missing ids, so don't force owner lookup on anonymous reads unless auth/governance is actually enabled. SDK/ASGI auth tests must override both `get_runtime` and `get_repository`/`app_state.repository` to the same repository; otherwise DB-key auth reads a different store from the runtime under test. Quota counter failures should fail closed only with governance enabled; disabled governance must not let optional Redis/quota failures affect benchmark defaults. Raw payload retention remains disabled unless governance is enabled and an encrypted raw store URL is configured; redaction state `blocked` must store no content.
- P4-D final-review findings (2026-06-14): session flush is a write-side side-effect boundary, so authorized flush must pass the authorized `workspace_id` down to `MemoryRuntime.flush_session(...)` / buffer `drain(...)` and must not drain another workspace sharing the same session id. Every route accepting both `run_id` and `workspace_id` should use one helper that returns 400 on mismatch and should execute with the same workspace used for authorization. Quota coverage must include step lifecycle and explicit flush writes, and async event writes must consume both `write_event` and `async_task_enqueue` quota units before enqueue. Secret-like metadata key hints must propagate through nested lists/dicts; otherwise short token/password values under `tokens`/`authorization` can persist. Replay/report exports are public observability surfaces: redact query text plus compaction summary/retained facts/warnings using both regex and secret-like key semantics. SDKs should map HTTP 403 to a dedicated `ForbiddenError`, and InMemory repository constraints should mirror SQL uniqueness for API-key prefixes. Governance migration/schema tests should cover `0011_governance` explicitly.
- Phase 4 full-review findings (2026-06-14): async event extraction needs durable event-level idempotency beyond worker TTL locks; duplicate `process_event_extraction(event_id)` calls must not create duplicate source-event memories. Workspace mismatch checks must run before any lazy flush or history folding side effects. Ordinary semantic `update_memory(...)` must not let stale copies re-activate lifecycle-managed rows; lifecycle/status changes should remain audit/CAS-owned. Do not persist bare SHA-256 digests of secret payloads because low-entropy values are dictionary-enumerable; use an operator-secret HMAC or omit digest. Replay redaction must match report redaction for retained negative evidence, not just retained facts. Dashboard version tables must include versions even when no conflict row references the memory. SDK `/v1` mirroring should include Phase 4 read APIs (`memory_versions`, `memory_conflicts`) and map both 401 and 403 to auth/permission errors.
- Integrations plan source-sync findings (2026-06-14): do not rely on stale pre-Phase-4 integration assumptions. Source routes show 401/403/429, workspace authorization, quota, path-authoritative `POST /v1/runs/{run_id}/complete`, workspace-scoped body `POST /v1/sessions/flush?workspace_id=...`, workspace-wide 403 for missing `workspace_id`, async `ExtractionMode` values, `WriteEventResult.queued/task_id/warnings`, and memory versions/conflicts are current HTTP contracts. TS SDK planning must cover these before MCP work; error parsing should understand FastAPI `{"detail":"..."}` and non-JSON failures; package names should be stable (`@memtrace/sdk`, `@memtrace/mcp-server`); MCP tools should call only the TS SDK and should cap/sanitize replay/report output by default rather than returning raw long JSON, API keys, Authorization headers, destructive commands, or `raw_payload_ref` values.
- INT-A TypeScript SDK findings (2026-06-14): Bun may not be installed globally in dev environments; temporary verification can use `npm exec --yes --package bun -- bun ...`, but the repository should still use Bun workspace metadata and `bun.lock`, not npm/pnpm/yarn lockfiles. With `exactOptionalPropertyTypes`, avoid passing explicit `undefined` for optional `RequestInit` fields or SDK constructor options; build objects conditionally instead. Typecheck examples as well as package tests so source examples cannot drift. Keep session flush body-based for arbitrary `session_id` values containing `/`, and preserve `completeRun(runId, req)` path-authoritative semantics even if the request body includes a different `run_id`. FastAPI request/query validation errors surface as 422 with `detail` arrays, so SDK bad-request mapping should cover both 400 and 422 rather than only manually raised 400 errors.
- INT-B MCP Server findings (2026-06-14): keep MCP tools as thin SDK calls and test that source files do not import Python runtime/database markers. MCP output is a chat/IDE surface, so never return raw replay/report JSON by default; cap long text (currently 8k chars) and redact auth headers, bearer/API keys, `sk-` tokens, password-like values, `raw_payload_ref`, destructive commands, and production path markers on both success and error paths. Detailed review found that JSON-style secret-like key/value pairs (for example `{"password":"hunter2"}` / `{"api_key":"plain-token"}`) are not covered by env/header-only redaction regexes; redact common JSON/env secret-like keys before generic token replacement and lock the regression with MCP tool tests. Unknown-tool errors are also user-visible MCP output and must use the same redacted/capped output path. `MEMTRACE_BASE_URL` config should be restricted to `http:`/`https:` and should reject embedded userinfo so credentials are not hidden inside base URLs. Package-local tests should avoid repo-root-relative source paths; use `import.meta.url`/absolute paths so `bun test test` and root-level test invocations both work. With `exactOptionalPropertyTypes`, build SDK request objects through conditional/undefined-omitting helpers; otherwise TypeScript rejects explicit `undefined` optional fields even though runtime JSON would omit them. Bun workspaces may not symlink workspace dependencies until `bun install` runs after adding the new package.
- INT-C MCP config template findings (2026-06-14): config templates are user-copyable secret surfaces, so tests should assert both structure and absence of real secret markers. Keep `examples/mcp/*.json`, exported `MCP_CONFIG_TEMPLATES`, and README snippets in sync; otherwise users copy stale config while tests pass against a different fixture. Use `${MEMTRACE_BASE_URL}` and `${MEMTRACE_API_KEY}` placeholders in JSON templates, with local defaults documented separately as shell `export` examples rather than embedded tokens. Do not create a dedicated IDE package until MCP adoption feedback shows editor-specific needs; otherwise the integration layer risks duplicating MCP/SDK semantics and creating another surface to keep redacted.
- INT-C final-review findings (2026-06-14): substring-only template tests are too weak because exported fixtures and example JSON can drift together; lock the exact local-dev template shape, including the allowed env key set. The checked-in MCP JSON templates are local-development templates: document that `packages/mcp-server/src/server.ts` is relative to the repository root, and tell users to replace it with an absolute path or installed package command if their MCP client launches elsewhere. `${...}` env placeholders are safer than inline secrets, but README must state that clients without placeholder expansion need rendered/replaced values outside version control. After completing integrations, search README/ROADMAP/umbrella plans for stale phrases that still list integrations as remaining work or leave old checklists unchecked without a completed/superseded banner.
- Cross-INT final-review findings before commit (2026-06-14): MCP redaction must treat JSON keys as the authoritative secret signal, not only secret-looking values. Cover common secret-bearing names beyond `password` / `api_key` / `access_token`: `authorization`, bare `token`, `client_secret`, `secret_key`, `id_token`, and suffixes such as `session_token`, `*_secret`, and `*_credential`, while avoiding over-redacting harmless fields such as `token_budget`. TS examples should import the public workspace package (`@memtrace/sdk`) rather than internal `packages/ts-sdk/src`; add TypeScript `paths` mapping if root typecheck cannot resolve the workspace package without a local install. Keep `AGENTS.md` production-code boundaries aligned when adding new packages, and mark historical superpowers plans completed/superseded so resume/search does not restart already-finished INT work.
- R1 release-readiness planning findings (2026-06-14): do command inventory before rewriting README/user docs, or public quickstarts can drift from real CLI/example/MCP commands and stable stdout markers. Do not overclaim demo wording; use actual test-locked markers rather than hard-coding old “baseline contamination” phrasing. Package metadata readiness should not silently become JS build-system work: keep `private: true` unless publishing is explicitly approved, and do not introduce `dist/`/bundler/declaration-emit pipelines by default. CI should use `oven-sh/setup-bun`, while docs may mention temporary Bun via `npm exec --yes --package bun -- bun ...` for local environments. Add a tracked-file release-hygiene guard for `node_modules`, `*.tsbuildinfo`, npm/pnpm/yarn lockfiles, `*.tgz`, generated reports, and local service artifacts. Do not advertise broad OTel/LangSmith/Phoenix/Langfuse support in public docs until the core exporter slice is completed and documented; Segment 1/2 internal exporter package work alone is not a public vendor-support claim.
- R1-A0/A1/A2 implementation findings (2026-06-14): classify TypeScript and MCP examples as local-service paths, not default quickstarts. `npm exec --yes --package bun -- bun examples/ts-simple-agent/src/index.ts` correctly fails with a connection error when the HTTP service is absent, so README/docs must state the service precondition instead of treating the example as no-network. README should not link `docs/release-checklist.md` until R1-C creates it. R1-A3 should reuse the stable no-network CLI/Python markers (`baseline_1 action`, `variant_2 action`, `contamination eliminated`) and keep HTTP/TS/MCP smoke env-gated.
- R1-A0/A1/A2 review findings (2026-06-14): SQL-backed quickstart docs should wait for the `memtrace-postgres` healthcheck before running Alembic, or first-time users can hit startup races. TypeScript example docs should show `context_block_count` only as a numeric smoke marker, not a stable API guarantee. MCP checked-in templates use `"command": "bun"`, so they require Bun on the MCP client's PATH; the shell-only temporary Bun fallback does not automatically apply inside most MCP clients. Do not say R1 “prepares package metadata” as if R1-B is done; say package metadata/package-shape checks are upcoming until R1-B completes.
- R1-A3/B1/B2 findings (2026-06-14): canonical public smoke should orchestrate existing no-network demos instead of inventing another scenario, and live HTTP/TypeScript checks must stay opt-in via env vars so default onboarding remains deterministic. Package metadata readiness is not publishing readiness: keep JS packages `private: true`, package source entries explicitly, and test `exports`/`files` shape without adding a `dist` build pipeline. Python package descriptions must not remain P0-only after SDK/CLI/platform work lands; metadata tests should lock README/license/URLs/classifiers and the `memtrace` console script so future package cleanup does not silently break CLI onboarding.
- R1-A3/B1/B2 review finding (2026-06-14): when adding a canonical smoke wrapper, update compact command tables as well as detailed sections. A README table that compresses SQL setup into `docker-compose up -d && alembic ...` can reintroduce the PostgreSQL startup race even if the detailed getting-started section has the health wait. Prefer linking from compact tables to the full health-waiting flow for service-backed paths.
- R1-B2 package-build finding (2026-06-14): package metadata tests that parse `pyproject.toml` are not enough for release readiness; run `uv build --out-dir /tmp/... --package <name>` as well. Hatchling rejects or cannot resolve `readme` paths outside the package root in the SDK package, so use an in-package README or inline PEP 621 readme metadata instead of `../../README.md` for `packages/python-sdk`.
- R1 full-regression README finding (2026-06-14): public landing-page rewrites must preserve machine-checked reproducibility entrypoint snippets, not only the friendly wrapper commands. `apps/api/tests/integration/test_reproducibility.py` expects README to mention `uv run python -m app.demo.run_demo`, `uv run python -m app.benchmark.runner`, `uv run python -m app.observability.reports`, `docker-compose.yml`, and `/v1/replay/access/{access_id}` so reviewers can find the underlying deterministic entrypoints and replay API.
- R1-B3/C2 release-hygiene finding (2026-06-14): tracked-file artifact guards should operate on `git ls-files`, not filesystem contents, because local `node_modules/`, generated `reports/`, `dist/`, and `*.tsbuildinfo` may exist untracked during development. Secret/destructive marker scans should target public docs/examples and exclude internal design/superpowers plans, because design docs and tests intentionally contain synthetic redaction fixtures such as `sk-...`, `password=hunter2`, and `rm -rf /prod`; scanning those as public leakage creates false positives without improving release safety. Keep default CI free of Postgres/Redis/LLM/live HTTP env requirements; those paths stay explicit/manual or env-gated.
- R1-B3/C2 review finding (2026-06-14): after later benchmark expansions, ROADMAP historical sections can still say “current acceptance 12/12” even though I7 raised the global suite to 13/13. When doing release closeout, distinguish “historical acceptance for that slice” from “current global benchmark/reproducibility acceptance” and update appendix wording so R1-C2 does not inherit stale counts.
- R1-C1/C2 release-closeout finding (2026-06-14): release readiness is not the same as automatic publishing. Keep release checklist steps explicit and manual unless maintainers approve package publication; JS packages should remain `private: true`, and R1 must not silently introduce `dist/`, bundlers, declaration emit, or publish workflows. After adding release docs, rerun both benchmark/reproduce and tracked-file hygiene, because generated `reports/` may change locally but should remain ignored/untracked while current global acceptance stays 13/13.
- R1 post-closeout review findings (2026-06-14): user-facing deployment docs can lag behind ROADMAP/`.ai` closeout and still claim future R1-B work; scan user docs as well as design/project-memory files after closeout. Historical 12/12 benchmark counts must say “historical slice-specific” anywhere outside explicitly old verification logs. If OTel/OpenInference is the next recommended feature, do not also list it under “far future / scale-only”. For package readiness, sibling Python packages should use a compatible version constraint rather than bare package names, and source-entry JS bins that require Bun should declare/test the Bun engine requirement while packages remain private.
- OpenTelemetry/OpenInference exporter planning findings (2026-06-14): telemetry is an external egress surface, so treat it like reports/MCP output but stricter. Build redaction and attribute budgeting before runtime hooks; preserve benign fields such as `token_budget` while redacting `authorization`, bare `token`, `client_secret`, `secret_key`, `id_token`, suffix `*_token`/`*_secret`/`*_credential`, `raw_payload_ref`, destructive commands, and production path markers. Keep exporter dependencies optional and imported lazily; default settings must be disabled/noop so CI, benchmark, reproduce, and quickstarts never require network telemetry. Runtime hooks must run only after authoritative persistence, fail open by default, and must not synchronously perform network OTLP export; use OTel SDK batching or an internal best-effort queue. Read-only export/backfill must not increment access counters, write gate/profile rows, rerun mutable retrieval side effects, or make telemetry a second source of truth. `memtrace.*` attributes are the stable contract; nested dict/list metadata should become capped JSON-string attributes before OTel conversion because OTel attributes are primitive/list-of-primitive oriented. JSONL exporter mode should default to deterministic overwrite, with append only when configured/documented. HTTP export endpoint and CLI telemetry-export are optional/second-batch surfaces, not core Segment 1/2 requirements. LangSmith/Phoenix/Langfuse should be documented only as possible OTLP/OpenInference destinations until vendor-specific bridges are explicitly selected.
- OTel/OpenInference Segment 1 implementation findings (2026-06-14): existing `is_secret_like_key(...)` treats any key containing `token` as sensitive, which would over-redact safe telemetry metrics like `token_budget` / `token_count`. Telemetry redaction needs an explicit benign-token allowlist while still redacting bare `token`, `session_token`, `id_token`, and secret suffixes. Destructive/prod markers should redact the whole string, not only replace matched substrings, because partial strings such as `run [REDACTED] [REDACTED] now` still reveal unsafe intent. Keep nested policy/provider snapshots as capped JSON-string attributes (`*_json`) before any future OTel SDK conversion; `TelemetrySpan`/`TelemetryEvent` should reject arbitrary dict attributes so regressions fail before export.
- OTel/OpenInference Segment 1 review findings (2026-06-14): do not let internal telemetry DTOs accept `None` as an attribute value even if Python callers use optional fields; omit `None` during sanitization before constructing spans/events. Redact string values that contain `raw_payload_ref` markers, not only dict keys named `raw_payload_ref`, because debug metadata can encode raw refs as `raw_payload_ref=...`. Benchmark telemetry must never export the full `metrics` dict: even redacted strings may leak ordinary raw prompt/context text, so project only an explicit allowlist of numeric/boolean benchmark metrics and acceptance flags.
- OTel/OpenInference Segment 1 final review findings (2026-06-14): builder-emitted token metrics can use both `input_tokens`/`output_tokens` and `token_input`/`token_output`; keep all benign metric spellings in the allowlist or event token counts will be redacted as secrets. Metadata dictionaries are not safe just because secret markers are redacted: raw-content-like keys such as `prompt`, `query`, `content`, `raw_context`, `messages`, and `context_blocks` must be treated as sensitive and projected only as presence/count/digest in future surfaces. Runtime DTOs like `MemoryAccessLog`, `ProfileEvent`, and `ReplayRetrievalResult` allow `run_id=None`; span trace-id builders must fall back to a record id (`access_id`/`profile_id`) rather than collapsing unrelated records into a shared `unknown` trace. Keep the OTel plan checklist synchronized after each segment so future sessions do not restart completed TEL-A/TEL-B work.
- OTel/OpenInference Segment 2 findings (2026-06-14): keep telemetry settings disabled/noop by default and factory construction side-effect-free; default settings must not create files, import OTel SDKs, or attempt network export. JSONL configured through settings should stay under `reports/...` and reject absolute paths, `..` traversal, and existing symlink path components; direct test exporters may still write to temporary paths. OTLP endpoint config must require HTTP(S) and reject embedded credentials, and missing endpoint/dependency cases should degrade to noop with warnings rather than failing local/dev paths. Exporter sanitization must cover both span attributes and nested telemetry event attributes before any in-memory, JSONL, or OTLP SDK conversion. `TelemetryService` should remain fail-open by default across both builder/projection and exporter stages; fail-closed behavior is explicit service-level testing only until runtime hooks exist. Segment 2 intentionally does not wire `MemoryRuntime`, API routes, or CLI; Segment 3 must preserve the rule that runtime hooks occur after authoritative persistence and do not synchronously perform network OTLP export.
- OTel/OpenInference Segment 2 detailed-review findings (2026-06-14): arbitrary runtime/profile metadata is not safe to export wholesale even after key/value redaction, because raw user/tool/model text can live under ordinary keys such as `message`, `input`, or `output`. Builders should project only coarse metadata facts such as `metadata.key_count` unless a specific safe allowlist is designed. OTLP URL parsing itself can throw on malformed URLs such as invalid IPv6 literals; factory validation must catch parser errors and degrade to noop with warnings rather than letting bad telemetry config break local/default paths.
- OTel/OpenInference Segment 3 findings (2026-06-14): runtime hooks must stay strictly post-persistence and fail-open; constructor-level telemetry injection is safer than global exporter lookup because tests and in-process runtimes can keep deterministic sinks. Retrieval telemetry should read persisted `MemoryAccessLog` / gate / profile / compaction rows after `retrieve_context(...)` returns; do not call replay or re-run retrieval for telemetry, or the export path can mutate access counters/logs and violate observability side-effect guarantees. HTTP telemetry export should return only counts/warnings; exposing raw spans would create another egress surface with different redaction expectations. CLI telemetry export remains deferred because adding it would expand SDK/HTTP client contracts and duplicate the explicit `/v1/telemetry/export/runs/{run_id}` surface.
- OTel/OpenInference Segment 3 detailed-review findings (2026-06-14): stable span ids make start+finish lifecycle exports ambiguous if both are emitted for the same run/step; runtime hooks should export terminal run/step snapshots once per lifecycle id (or future start/finish events must use distinct span ids/names). Runtime hooks need their own unconditional fail-open boundary, because `TelemetryService(fail_open=False)` is valid for explicit service tests/admin export but must never break already-persisted `MemoryRuntime` hot paths. Treat `/v1/telemetry/export/runs/{run_id}` as an export/report egress surface: enforce existing `QuotaUnit.report_export` after report-reader authz, return counts/warnings only, and keep access-level export/backfill plus CLI telemetry-export deferred until they have independent redaction/capping/authz/quota tests. Project memory should not keep OTel listed as the selected next target after Segment 4 closeout.
- OTel/OpenInference Segment 4 closeout findings (2026-06-14): after exporter closeout, update both the OTel plan and broader roadmap/project-memory wording; otherwise resume flows can keep treating Segment 4 as pending even after verification passes. Optional OTLP collector smoke should remain optional unless a local collector is actually part of the dev environment; default verification can rely on factory/exporter degradation tests plus no-network regression, benchmark, reproduce, release hygiene, and diff checks. If future work adds CLI telemetry export or access/backfill endpoints, treat them as new egress surfaces with their own redaction/capping/authz tests rather than assuming the run-export endpoint coverage is enough.
- OTel/OpenInference final full-plan review findings (2026-06-14): do not rely on the settings factory as the only JSONL safety boundary; direct `JsonlTelemetryExporter` construction and write-time behavior must also reject absolute/parent escapes and symlink swaps. Public telemetry warnings are an egress surface too: log full exceptions internally, but return generic warnings because exporter exceptions can include span JSON or raw attributes. Runtime telemetry hooks must wrap their own projection reads as well as service/exporter calls; otherwise a telemetry-only `get_access_log(...)` failure can break successful retrieval. Stable run/step span ids require exporting terminal lifecycle snapshots only on first non-terminal → terminal transition, because idempotent `finish_step` / `complete_run` calls otherwise duplicate span identity. After closeout, search historical `.ai`/ROADMAP text for "current next" wording, not only top-level current-state sections.
- Maintenance/admin governance planning findings (2026-06-14): admin APIs are an operator surface and must be default-off (`MEMTRACE_ADMIN_API_ENABLED=false`) with owner-gated mutation when enabled; first-wave admin must reject anonymous principals entirely rather than adding a local anonymous bypass. Scheduler/admin outputs are egress/audit surfaces: redact raw API keys, auth headers, raw payload refs, destructive commands, production paths, and raw memory/event content from run summaries, task errors, admin audits, dashboard rows, and reports. Automatic maintenance must be conservative: `dedup_memory` may only supersede exact same canonical key/scope/value duplicates and must not touch pinned/quarantined/conflicted/secret/redaction-managed/different-value memories; dry-run must report candidate pairs without mutation; conflict resolution that chooses a winner is an explicit admin action and the winner must be in the conflict's same-workspace memory ids. `reindex_memory` in the first pass must stay deterministic/local (`provider_backed=false`) and not call real embedding providers, or benchmark/reproduce can become network-dependent. PostgreSQL `quota_limits` cannot rely on a normal unique constraint over nullable `principal_id`; use partial unique indexes for workspace-wide vs principal-specific overrides. Maintenance task attempts should be unique by `(scheduler_run_id, operation)` with per-run `idempotency_key`, not a cross-run `dedupe_key`. `summary_refresh` / `procedural_refresh` must reuse/refactor the existing `MemoryRuntime.complete_run(...)` summary/procedural helper path instead of directly constructing memories and bypassing summarizer/source/embedding/version/conflict semantics. Keep admin routes in a separate router to avoid further growing `api/routes.py`, but reuse existing workspace/resource authorization helpers and lifecycle/versioning/conflict helpers rather than inventing parallel semantics.
- MADM-A implementation findings (2026-06-15): keep admin settings separate from existing `auth_enabled` / `governance_enabled`; `admin_api_enabled=False` should hide admin routes/helpers with 404, while enabled admin must reject `Principal(kind="anonymous")`, legacy/global principals, persisted DB API keys scoped to wildcard `workspace_ids=["*"]`, and synthetic `kind="api_key"` principals without `api_key_id` even if owner-like wildcard roles are present. Admin metadata redaction must treat key names as authoritative for secrets/raw payload refs, not only regex values, and must also redact `raw_payload_ref`, destructive commands, and production-path strings. Maintenance task-attempt uniqueness belongs to `(scheduler_run_id, operation)`; do not make `idempotency_key` globally unique or later scheduler runs will collide. Updates must not let an existing task attempt move across `(scheduler_run_id, workspace_id, operation)` or overwrite `created_at`, because that breaks run history/audit ordering even when the uniqueness constraint is not violated. Quota override SQL needs two partial unique indexes (`principal_id IS NULL` and `principal_id IS NOT NULL`); in-memory replacement logic should mirror the same identity so tests catch nullable-unique drift before PostgreSQL. Quota upsert must preserve `(workspace_id, principal_id, unit)`, `created_by`, and `created_at` for an existing override id/natural identity; never let reuse of a `quota_limit_id` silently move a quota override to another workspace/principal/unit. Extending admin-facing list methods with `limit/offset` should preserve existing zero-argument call compatibility for older runtime/API tests.
- MADM-B implementation findings (2026-06-15): `dedup_memory` needs both lifecycle transition semantics and lineage update; repository `transition_memory_with_audit(...)` must persist `superseded_by` alongside status/lifecycle metadata or duplicate rows become `superseded` without pointing to the survivor. Test stale embeddings carefully: `Repository.add_memory(...)` auto-backfills missing vectors and marks them embedded, so `reindex_memory` stale-status tests should seed a stale vector/status through an update path or explicit vector. Celery maintenance wrappers should not maintain a separate operation handler map; delegate to `run_workspace_maintenance(...)` so direct and Celery paths share operation registry, dry-run behavior, attempt records, redaction, and failure isolation. Summary/procedural refresh should stay completed-run-only and create only missing output; it must not mutate running/failed/cancelled runs or rewrite existing run summaries/procedures during scheduler maintenance.
- MADM-B review findings (2026-06-15): a durable maintenance run can fail without the Celery wrapper raising, because the orchestrator intentionally isolates per-operation exceptions; the wrapper must inspect `SchedulerRunStatus.failed`, return a failed task result, and release the task-envelope dedupe key for retry. Scheduler/admin result dictionaries are persistent egress surfaces: do not store raw duplicate `value` fields in dedup candidate pairs, even when values are non-secret; use ids/digests/reasons instead. `conflict_scan` should only stale-resolve conflicts owned by its detector (`maintenance_conflict_scan_v1`), otherwise it can close runtime/manual open conflicts. Summary/procedural refresh existence checks should ignore non-retrievable lifecycle statuses such as superseded/archived/deleted so maintenance can rebuild an active run memory when the only matching record is stale.
- MADM-B full-review findings (2026-06-15): maintenance-internal list usage must not rely on admin-facing default pagination. `conflict_scan` has to paginate all open maintenance-owned conflicts before stale resolution, or conflicts beyond the first page remain open forever. Secret-like `MemoryItem.key` values are sensitive even when `sensitivity` / `risk_flags` were not set, so `dedup_memory` must skip them and avoid echoing canonical secret-like keys in results. Summary/procedural refresh can encounter legacy same-key active rows with missing or mismatched `source_run_id`; supersede exact same-key active rows before adding a replacement to preserve the complete-run single-active-key semantics. Orchestrator run-level fields (`requested_by`, `reason`) are durable egress/audit surfaces and need the same admin redaction as attempt results/errors; free-form strings containing `Authorization:`-style assignments should redact even when token regex length thresholds do not match. `update_maintenance_run(...)` should preserve immutable run identity/creation fields (`workspace_id`, `requested_by`, `reason`, `operations`, `dry_run`, `created_at`) just like task attempts and quota overrides, rather than merging whole-row caller objects during status updates.
