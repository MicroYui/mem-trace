# Requirements

## Current Task

Current task (2026-06-21): Phase 3-B Showcase Dashboard from `docs/design/PHASE3B_DASHBOARD_PLAN.md` is complete through WEB-A..WEB-J and has now passed a full pre-commit verification sweep. The final check revalidated Bun workspace install, WEB-H/I/J targeted tests, all web tests, root TypeScript typecheck, root Bun tests, production web build plus unsafe-marker dist scan, Python 3.12 compileall, full Python pytest, release hygiene, whitespace checks, and fixture screenshots. The only new fix in this final sweep was a `docs/getting-started.md` heading correction so dashboard fixture mode is Track 4 and benchmark/reproducibility remains Track 5. The next roadmap target should be selected deliberately rather than continuing Phase 3-B by default.

P2 is feature-complete (6/6 mvp.md §2.3 slices, incl. the config-gated LLM extraction pipeline) and committed. Phase 3-A backend observability is complete. **Context Compaction (ROADMAP §9) is complete through C5 plus C6/I7 retained-negative metadata closeout**, per `docs/design/CONTEXT_COMPACTION_PLAN.md`. **Failure-aware Negative Memory Injection I1-I7 are complete**, per `docs/design/FAILURE_AWARE_NEGATIVE_MEMORY_PLAN.md` and `docs/design/I7_COMPACTION_NEGATIVE_RETENTION_PLAN.md`. **Phase 3.5 SDK/LangGraph adapter/CLI is complete through S6**, per `docs/design/SDK_ADAPTER_PLAN.md`. **ROADMAP §7 "完整 6 策略对比 + benchmark 落库" is complete through Task 11**, with six strategies, eval-table persistence, dashboard surfacing, and reproducibility closeout; I7 has since expanded the deterministic benchmark to **13 cases × 6 strategies = 78 rows** and `acceptance.passed=true (13/13 checks true)`. **ROADMAP §13 Security & Consistency Hardening is complete through H18, post-review hardened, and verified with full regression**. **ROADMAP §10 Provider Registry + §11 Controlled Memory Key Ontology is complete through P10**. **Phase 4 via `docs/design/PHASE4_PLATFORM_PLAN.md` is complete through P4-D4 and final P4-D review hardening.** **Integrations INT-A TypeScript SDK is complete:** Bun workspace, `@memtrace/sdk`, strict TypeScript DTOs/client/errors, current Phase 4 HTTP surface coverage, mock contract smoke, optional real-service smoke, and TS example are implemented. **Integrations INT-B MCP Server is complete:** `@memtrace/mcp-server` provides env-configured stdio MCP tools over the TS SDK, first-wave and second-wave tools, concise redacted outputs, 8k replay/report caps, README MCP config snippets, and SDK-only/no-Python-import tests. **Integrations INT-C MCP config templates / IDE thin-layer decision is complete:** Claude Code/Cursor templates and tests use `MEMTRACE_BASE_URL` / `MEMTRACE_API_KEY` env placeholders with no real secrets; dedicated IDE package remains deferred until MCP adoption feedback. **R1 Release Readiness & Public Adoption is complete:** command inventory, README public landing page, user docs, release checklist, canonical release-readiness smoke, TypeScript package metadata/package-shape checks, Python package metadata/CLI readiness, default GitHub Actions CI, release hygiene, benchmark/reproduce closeout, and ROADMAP/`.ai` sync are implemented. **OpenTelemetry/OpenInference exporter core slice, Maintenance Scheduler / Admin Governance Depth, and Phase 3-B Showcase Dashboard are complete. Selected next target:** none.

### Phase 3-B Showcase Dashboard Requirements (completed plan)

- Use `docs/design/PHASE3B_DASHBOARD_PLAN.md` as the authoritative implementation plan.
- Goal: build a polished, memorable React + TypeScript dashboard in `apps/web` that makes MemTrace's trace/state/gate/replay/benchmark differentiator visible for demos, screenshots, and open-source adoption.
- Architecture: separate Bun workspace frontend over `@memtrace/sdk` and existing read-only `/v1` APIs. The existing `/v1/dashboard/ui` remains a small static viewer and must not be expanded into the full frontend.
- Primary views: Overview/run gallery, Run Explorer, Access Replay + Memory Flow, Benchmark Lab, Memory Atlas, owner-gated read-only Ops, and fixture-backed Showcase mode.
- Data boundary: use `GET /v1/dashboard/tables`, observability summary, timeline/state-tree/steps/profile, inspect/replay, memory versions/conflicts, and existing report/telemetry export routes only for explicit export actions. Add backend routes only if a bounded read-only projection is required.
- Durable constraints: no runtime semantic changes; no Phase 5 ES/Neo4j/Query Planner/multi-hop work; no admin mutation UI in the first pass; no API keys in URLs/logs/fixtures; no untrusted HTML rendering; no telemetry from the dashboard by default; preserve authz/quota/redaction/workspace isolation for every data surface.
- Required design hardening before feature pages grow: route/page components consume typed view models from `api/normalizers.ts` / `api/viewModels.ts`, not raw DTOs; live and fixture data share the same normalizers; owner-gated tables use a shared `CapabilityState` and distinguish forbidden/owner-only-unavailable/authorized-empty/unsupported/unknown states; `VITE_MEMTRACE_API_BASE_URL` defaults to same-origin with dev proxy and documented CORS/reverse-proxy behavior; detail routes are independently loadable; Overview metrics do not infer event counts, context-block counts, compaction status, or unsafe-leakage aggregates without explicit projections or documented normalizer-derived display groups; benchmark metrics never infer missing values and UI logic does not hardcode the current 13 cases; `degrade` is warning-only negative evidence, not positive accepted context; Memory Atlas applies content-display grading and recursively redacts nested memory-version snapshots; fixtures include schema/version metadata and validation; workspace selection does not require a workspace listing API. ✅ Complete through WEB-J.
- First slice acceptance: `apps/web` exists as a workspace app; fixture/live connection shell renders without a live API; root TS typecheck includes web code; production build passes; existing TS SDK/MCP package tests remain intact.
- WEB-A/WEB-B implementation status: complete and review-hardened as of 2026-06-17. `apps/web` is a Bun workspace React/Vite/TypeScript app over `@memtrace/sdk`; fixture/live mode share view-model normalizers; query hooks cover the planned read-only API surfaces; fixture schema validation, `CapabilityState`, missing-metric semantics, API-key header behavior, query keys, and error-state mapping have test coverage. Review hardening rejects protocol-relative API base URLs before API keys can be sent cross-origin, accepts `/` as same-origin root, rejects embedded credentials, and explicitly types dashboard maintenance/admin/quota rows in the TS SDK. WEB-C/WEB-D implementation status: complete. Reusable drawer/score/timeline/token/strategy/error primitives are extracted, Overview renders a real run gallery/access stream/strategy comparison/safety-compaction-negative signal surface over normalized view models, and tests cover the new primitives plus explicit metric semantics. WEB-E/WEB-F implementation status: complete and review-hardened as of 2026-06-20. Direct `/runs/:runId` and `/access/:accessId` pages load route-specific fixture/live data through shared normalizers; Run Explorer renders timeline/state/steps/profile/detail evidence; Access Replay renders memory flow, candidate/gate matrix with final and component score bars, context blocks, negative evidence, and replay drift while keeping `degrade` out of accepted positive context. WEB-G implementation status: complete and review-checked as of 2026-06-20. Direct `/benchmark` loads fixture/live dashboard-table data through `normalizeBenchmarkLab(...)`; it renders all returned benchmark/eval cases across six strategies, contamination/token-bloat/reflection/compaction/negative-evidence panels, explicit not-run/unavailable/comparator-unavailable states, and case detail source metrics/links without hardcoding the current 13 cases into UI logic. WEB-H/WEB-I/WEB-J implementation status: complete as of 2026-06-20. Direct `/memories` combines `GET /v1/memories` with dashboard-table versions/conflicts through `normalizeMemoryAtlas(...)`; direct `/ops` renders owner-gated maintenance/admin/quota read-only tables through `normalizeOpsReadOnly(...)`; direct `/showcase` provides the fixture walkthrough and screenshot workflow. Recursive version-snapshot redaction and ops capability states are covered by tests.

### Maintenance Scheduler / Admin Governance Depth Requirements (completed plan)

- Use `docs/design/MAINTENANCE_ADMIN_GOVERNANCE_PLAN.md` as the authoritative implementation plan.
- Goal: complete the post-Phase-4 maintenance/admin depth slice without changing default local/benchmark behavior.
- Scope: durable `MaintenanceRunRecord` / `MaintenanceTaskAttemptRecord` / `AdminActionAuditRecord` / `QuotaLimitRecord` with no distributed lease in this slice; first-wave maintenance operations (`score_memory`, `decay_memory`, `archive_memory`, `quarantine_memory`, `conflict_scan`, `dedup_memory`, `reindex_memory`, `summary_refresh`, `procedural_refresh`, `profile_refresh`); unified direct/admin/Celery orchestrator; owner-gated admin HTTP APIs for maintenance runs, lifecycle audits/manual transitions, conflict resolution, API key create/list/revoke, and quota limit overrides; dashboard/report surfacing; docs/ROADMAP/`.ai` closeout.
- Durable constraints: admin API default-off via `MEMTRACE_ADMIN_API_ENABLED=false`; mutating admin APIs require a real authenticated persisted per-workspace `WorkspacePermission.owner` API-key principal when enabled; anonymous, legacy/global, wildcard DB-key, and synthetic API-key admin access are not supported in this slice; Redis/Celery remain optional; no network provider dependency in default verification; all semantic memory status changes go through lifecycle audit/versioning helpers; no raw API key, auth header, raw payload ref, destructive command, production path, or raw memory/event content may appear in scheduler/admin outputs; quota workspace-wide overrides use partial unique indexes because nullable `principal_id` cannot be protected by an ordinary PostgreSQL unique constraint; quota override identity and creation audit fields are immutable across upserts; admin list APIs use bounded `limit/offset` pagination.
- Non-goals: no React/admin UI, no JWT/OIDC/membership table, no Elasticsearch/OpenSearch/Neo4j, no production scheduler-beat requirement, no encrypted raw payload store implementation, and no broad SDK admin facade in the first implementation pass.
- Execution order: MADM-A foundations (settings/gating + records/migration/repository) ✅ complete; MADM-B maintenance engine/orchestrator ✅ complete; then MADM-C admin maintenance API, MADM-D1 API key/quota admin, MADM-D2 lifecycle/conflict admin, MADM-E observability/docs/full closeout.

### MADM-A Foundations Progress Completed (2026-06-15)

- Task 1 added default-off `admin_api_enabled`, maintenance operation settings, and `app.governance.admin` helpers. `require_admin_api_enabled(...)` returns HTTP 404 while disabled; `require_admin_owner(...)` rejects anonymous, legacy/global, wildcard DB-key, synthetic API-key, and non-owner principals with HTTP 403 when enabled; `redact_admin_metadata(...)` recursively redacts secret-like keys/values, raw-payload keys/markers, destructive commands, and production-path markers from admin/scheduler egress surfaces.
- Task 2 added `MaintenanceOperation`, `SchedulerRunStatus`, `SchedulerTaskStatus`, `MaintenanceRunRecord`, `MaintenanceTaskAttemptRecord`, `AdminActionAuditRecord`, and `QuotaLimitRecord`; repository protocol/in-memory methods for maintenance runs, task attempts, admin audits, and quota overrides; pagination extensions for lifecycle audits, memory conflicts, and API keys; SQL ORM/mappings; and migration `0012_maintenance_admin_governance`.
- Storage invariants are locked: duplicate operations in one maintenance run are rejected; task attempts are unique per `(scheduler_run_id, operation)`, cannot move across `(scheduler_run_id, workspace_id, operation)`, preserve `created_at`, and do not use cross-run `dedupe_key`; quota override identity is `(workspace_id, principal_id|NULL, unit)`, cannot be moved by reusing an existing `quota_limit_id`, preserves `created_by` / `created_at` on update, and PostgreSQL uses partial unique indexes for nullable `principal_id`; JSONB defaults use explicit `sa.text("'{}'::jsonb")` / `sa.text("'[]'::jsonb")`.
- Post-review hardening fixed legacy/global/wildcard/synthetic API-key admin rejection, raw-payload-ref metadata key redaction, quota upsert id/identity/creation-audit consistency, task-attempt update uniqueness/identity immutability, and bounded pagination validation across in-memory/SQL repositories. Verification: admin helper tests **8 passed**; maintenance repository tests **12 passed**; combined admin+maintenance tests **20 passed**; storage migration tests **21 passed, 1 skipped**; targeted governance+memory+storage suite **56 passed, 1 skipped**; full pytest **680 passed, 2 skipped**; compileall and `git diff --check` passed.

### MADM-B Maintenance Engine Progress Completed (2026-06-15)

- Task 3 added expanded first-wave maintenance operations: `conflict_scan`, `dedup_memory`, `reindex_memory`, `summary_refresh`, and `procedural_refresh`. `conflict_scan` upserts current detected conflicts and resolves stale open conflicts; `dedup_memory` only supersedes safe active exact same canonical key/scope/value duplicates and reports dry-run candidate pairs without lifecycle mutation; `reindex_memory` uses deterministic local `stable_embedding(..., 256)` only and reports `provider_backed=false`; summary/procedural refresh creates missing completed-run memories through existing summarizer output semantics; `profile_refresh` remains read-only.
- Task 4 added `app.memory.maintenance.run_workspace_maintenance(...)` with an operation registry over all first-wave operations, durable `MaintenanceRunRecord` / `MaintenanceTaskAttemptRecord` updates, ordered execution, dry-run skipped attempts, per-operation failure isolation, redacted error summaries, and summary counters. The Celery `maintenance.memory` wrapper now delegates to the orchestrator and supports both legacy single `operation` and new `operations` list payloads while preserving task-envelope idempotency.
- Review hardening fixed failed durable maintenance runs being reported as successful Celery tasks, raw duplicate values appearing in dedup candidate pairs, maintenance conflict scans resolving non-maintenance conflicts, summary refresh skipping rebuilds when only superseded/deleted run summaries existed, paginated stale-conflict misses beyond the default list page, secret-like memory keys entering dedup previews/automation, legacy same-key active run summary/procedure duplicates, run-level `requested_by`/`reason` redaction gaps, and maintenance-run identity/creation-field mutation during status updates.
- Verification: RED MADM-B tests reproduced missing scheduler operations, missing orchestrator module, and old Celery metadata/operations-list behavior; review RED tests reproduced the hardening issues above. Final targeted suites: maintenance orchestrator **28 passed**, Celery eager **11 passed**, admin helper redaction **8 passed**; affected lifecycle/storage/memory/Celery/governance suite **95 passed, 1 skipped**; full pytest **698 passed, 2 skipped**; reproduce preserved **13/13** acceptance; compileall and `git diff --check` passed.

### OpenTelemetry / OpenInference Exporter Requirements (completed plan)

- Use `docs/design/OTEL_OPENINFERENCE_EXPORTER_PLAN.md` as the authoritative completed implementation record.
- Goal: add a default-off telemetry export layer that maps MemTrace runs, steps, events, retrieval accesses, gate decisions, profiler phases, replay/report summaries, and benchmark outcomes into redacted OpenTelemetry/OpenInference-compatible spans/events.
- Architecture: pure semantic builders + recursive redaction/budgeting + pluggable exporter protocol + `TelemetryService` facade. Exporters include noop default, deterministic in-memory test sink, safe JSONL local sink, and optional OTLP sink when dependencies and endpoint settings are present. `memtrace.*` attributes are the stable contract; OpenInference-compatible fields are a documented compatibility layer only.
- Runtime hooks must be best-effort and fail-open by default; telemetry export must occur after authoritative persistence succeeds and must not mutate repository state, access counters, gate logs, profile rows, replay state, or benchmark results. Runtime hooks may build/enqueue spans but must not synchronously perform network OTLP export; OTLP delivery must use SDK batching or an internal best-effort queue. Runtime hot-path hooks must remain fail-open even when a fail-closed `TelemetryService` is injected for explicit service tests/admin export, and run/step lifecycle hooks must avoid duplicate span ids for the same logical lifecycle.
- Export/backfill APIs are read-only projections over persisted records. PostgreSQL runtime records remain the source of truth; telemetry spans are not a second source of truth. The implemented surface is minimal run export; access-level export and richer backfill remain deferred.
- Redaction is mandatory before external egress: no raw event content, raw memory content, raw failed-attempt text, API keys, auth headers, `raw_payload_ref`, destructive commands, production path markers, or secret-like metadata may leave through telemetry.
- Keep optional telemetry network export gated: default CI, benchmark, reproduce, release-readiness smoke, and public quickstart must not require OTLP endpoints, external SaaS, Redis/Celery, LLM providers, or live HTTP.
- LangSmith/Phoenix/Langfuse remain OTLP/OpenInference-compatible destination examples only in this slice; do not add vendor SDK dependencies or claim direct vendor support until a separate bridge is implemented.
- Plan execution order is four segments: Segment 1 contracts/redaction/pure builders ✅; Segment 2 exporters/settings/factory/service ✅; Segment 3 runtime hooks plus docs and minimal HTTP run export ✅ and detailed-review hardened for fail-open/no-duplicate-span/quota behavior; Segment 4 verification and closeout ✅. CLI telemetry-export remains deferred.
- Required closeout: telemetry targeted suite, affected observability/runtime regression, compileall, full pytest, JS checks if JS/docs examples change, benchmark/reproduce non-regression at current global 13/13 acceptance, release hygiene, `git diff --check`, ROADMAP/`.ai` sync, and pitfalls update.

### R1 Release Readiness Requirements (completed plan)

- Use `docs/design/RELEASE_READINESS_PLAN.md` as the authoritative completed R1 implementation record.
- R1 goal: make MemTrace externally understandable, runnable, connectable, reproducible, and release-ready without reading internal design history.
- R1 must not add or change runtime retrieval/gate/context-packing semantics; it packages existing capabilities into README/user docs, deterministic demos/smoke checks, package metadata, CI, release checklist, and project-memory closeout.
- Execute in this order: R1-A0 command inventory ✅; R1-A1 README landing-page rewrite ✅; R1-A2 thin user docs ✅; R1-A3 5-minute demo/smoke consolidation ✅; R1-B1 TypeScript package metadata/package-shape checks ✅; R1-B2 Python package metadata/CLI readiness ✅; R1-B3 CI matrix ✅; R1-C1 release checklist ✅; R1-C2 full verification, release hygiene, ROADMAP, and `.ai` sync ✅.
- README/docs quickstarts may only present default/no-network commands as unconditional; local-service, Docker/PostgreSQL, Redis/Celery, live HTTP, and external-provider paths must state preconditions.
- Demo docs must use actual stable output markers discovered during R1-A0, not overclaim old wording such as a literal “baseline contamination” phrase unless test-locked.
- TypeScript package readiness should add metadata/exports/files and package-shape checks while keeping `private: true` unless publication is explicitly approved; do not introduce a `dist/` build, bundler, `tsup`, declaration emit, or package-layout migration by default.
- CI should use `oven-sh/setup-bun`; local docs may mention the temporary Bun fallback `npm exec --yes --package bun -- bun ...` for environments without global Bun.
- Add or run a release-hygiene tracked-file guard for forbidden artifacts (`node_modules`, `*.tsbuildinfo`, npm/pnpm/yarn lockfiles, package tarballs, generated reports, local service artifacts) and secret-marker scans for docs/examples.
- R1 non-goals: OpenTelemetry/OpenInference exporter, Phase 5 advanced retrieval/storage, React dashboard, admin/manual conflict workflow, dedicated IDE extension, automatic npm/PyPI publishing, Redis/Celery/LLM/live-service requirements in default CI.
- Historical target after R1: OpenTelemetry/OpenInference exporter. This has since completed through Segment 4 closeout, and Phase 3-B Showcase Dashboard is also complete through WEB-J. No next roadmap target is currently selected.

### R1-A0/A1/A2 Progress Completed (2026-06-14)

- R1-A0 command inventory confirmed the actual no-network stable markers: `baseline_1 action: npm test (contamination=1)`, `variant_2 action: bun test (contamination=0)`, and `contamination eliminated: true` from both CLI in-process demo and `examples/simple_agent`.
- README/docs classify runtime requirements: CLI/Python demos, benchmark, and reproduce are default/no-network; HTTP service requires Docker/PostgreSQL; CLI HTTP demo, TypeScript SDK example, and MCP server require a running local service; Redis/Celery and real providers remain optional/env-gated.
- R1-A1 rewrote README as a public landing page with product positioning, plain-vector-memory contrast, implemented capability summary, no-network quickstart, quickstart command table, HTTP/TS/MCP preconditions, benchmark/reproducibility, and user-doc links.
- R1-A2 created `docs/getting-started.md`, `docs/concepts.md`, `docs/mcp.md`, `docs/benchmark.md`, and `docs/deployment.md`.
- Verification completed: CLI demo passed, Python example passed, deterministic benchmark passed with 13/13 acceptance, TypeScript typecheck passed, and MCP config template tests passed.

### R1-A3/B1/B2 Progress Completed (2026-06-14)

- R1-A3 added `scripts/smoke-release-readiness.sh` as the canonical no-network demo smoke. It verifies CLI in-process and Python SDK example markers and keeps HTTP/TypeScript live-service checks opt-in via `MEMTRACE_SMOKE_HTTP_URL`, `MEMTRACE_SMOKE_TS=1`, and `MEMTRACE_BASE_URL`.
- R1-B1 added release-ready package metadata, source-entry `exports`, explicit `files`, and package-shape tests for `@memtrace/sdk` and `@memtrace/mcp-server`; both remain `private: true` and no `dist`/bundler/declaration pipeline was introduced.
- R1-B2 updated root `memtrace` and `memtrace-sdk` package metadata with current descriptions, Apache-2.0 license, README, URLs, classifiers, and metadata tests while preserving the `memtrace` console script.
- Post-review hardening fixed the README quickstart table to link to the full health-waiting Local HTTP flow instead of showing a race-prone one-line Docker/PostgreSQL command, fixed `memtrace-sdk` package build by replacing an out-of-package README path with inline PEP 621 readme metadata, and restored README reproducibility entrypoint snippets required by `apps/api/tests/integration/test_reproducibility.py`.
- Verification completed: targeted Python release tests **10 passed**; package-shape Bun tests **4 passed**; TypeScript root typecheck passed; root Bun tests **31 passed, 1 skipped**; release-readiness smoke passed; CLI demo passed; root and SDK `uv build --out-dir /tmp/...` passed; full pytest **607 passed, 2 skipped** after README reproducibility fix; deterministic benchmark passed with `acceptance.passed=true` and **13/13 checks true**; tracked artifact hygiene guard passed.

### R1-B3 / R1-C2 Step 4 Progress Completed (2026-06-14)

- R1-B3 added `.github/workflows/ci.yml` with three default jobs: Python compile/full pytest, Bun typecheck/tests via `oven-sh/setup-bun@v2`, and release hygiene.
- Default CI deliberately does not require `MEMTRACE_TEST_DATABASE_URL`, `MEMTRACE_TEST_REDIS_URL`, LLM/provider credentials, `MEMTRACE_BASE_URL`, or a live HTTP service.
- R1-C2 Step 1/2 were also rerun during the R1-B3 review: Python compileall passed, full pytest **607 passed, 2 skipped**, TypeScript root typecheck passed, and root Bun tests **31 passed, 1 skipped**.
- R1-C2 Step 4 added executable `scripts/check-release-hygiene.sh`, covering tracked generated/local artifacts plus public docs/examples scans for obvious real secrets, bearer/sk tokens, unredacted `raw_payload_ref`, and raw destructive production commands while allowing explicit placeholders/env-var references.
- Verification completed: `bash -n scripts/check-release-hygiene.sh` passed; `bash scripts/check-release-hygiene.sh` -> `release hygiene checks passed`; `git diff --check` over touched files passed.

### R1-C1 / R1-C2 Closeout Completed (2026-06-14)

- R1-C1 added `docs/release-checklist.md` with release scope selection, required local verification, Python/JS package dry-run checks, artifact/secret hygiene, tagging notes, explicit publish decision gates, and rollback guidance. README now links the release checklist from the user-docs section.
- R1-C2 Step 3 reran deterministic benchmark and reproducibility closeout: `uv run python -m app.benchmark.runner --output-dir reports` exited 0 and generated `reports/benchmark_results.json` with `acceptance.passed=true` and **13/13** true checks; `bash scripts/reproduce.sh` exited 0 and printed `acceptance.passed=true (13/13 checks true)`.
- R1-C2 Step 4 release hygiene was rerun after the checklist/docs changes: `bash scripts/check-release-hygiene.sh` -> `release hygiene checks passed`.
- R1-C2 Step 5 synced `docs/design/RELEASE_READINESS_PLAN.md`, `docs/design/ROADMAP.md`, and `.ai` memory to mark R1 complete and historically identify OpenTelemetry/OpenInference exporter as the next feature slice; that exporter slice is now complete.
- Final closeout verification after docs/memory sync: `git diff --check` passed; `uv run --extra dev pytest -q` -> **607 passed, 2 skipped**; `npm exec --yes --package bun -- bun run typecheck` passed; `npm exec --yes --package bun -- bun test` -> **31 passed, 1 skipped**.
- Post-closeout detailed review hardening fixed stale deployment/ROADMAP/memory wording, clarified historical 12/12 acceptance vs current global 13/13, pinned `memtrace-sdk`'s sibling dependency to `memtrace>=0.1.0,<0.2.0`, and declared/tested the MCP package Bun runtime requirement. Verification after hardening: Python metadata/smoke tests **3 passed**, package-shape Bun tests **4 passed**, release hygiene passed, `git diff --check` passed, full pytest **607 passed, 2 skipped**, TypeScript typecheck passed, and full Bun tests **31 passed, 1 skipped**.

### I7 Compaction Negative Retention Requirements (completed plan)

- Preserve failure-aware negative lessons through compaction metadata, replay, observability, reports, trace bundles, and benchmark acceptance without changing positive prompt-context semantics.
- Add `RetainedNegativeEvidence` as a dedicated safe metadata DTO derived only from `NegativeEvidence.safe_text`; do not read raw `MemoryItem.content` during retained conversion.
- Add `ContextCompactionLog.retained_negative_evidence` / `PendingCompactionLog.retained_negative_evidence` as a dedicated field; do not overload positive `retained_facts`.
- Persist the new field as JSONB with explicit PostgreSQL default `sa.text("'[]'::jsonb")`; SQL writes use `model_dump(mode="json")`, SQL reads use `RetainedNegativeEvidence.model_validate(...)`, and old `None`/missing rows map to `[]`.
- Set the I7 Alembic migration `down_revision` to the actual current head revision id from `migrations/versions`, not an inferred filename prefix.
- Keep `risk_kind` tolerant (`str | None`) or normalize known aliases without validation crashes.
- Packer retention only processes dropped standard negative-evidence prompt blocks where `block.type == "avoided_attempts"` **and** `block.source == "negative_evidence"`; retained metadata must never force the block into prompt context. Retained negative lookup must use `negative_by_memory_id` / `negative_by_state_reason`, not accepted-only `memory_by_id`, and `build_negative_evidence_block(...)` should preserve `source_state_node_id` via fallback provenance when `ev.provenance` is absent.
- Replay reads retained negative evidence directly from compaction logs, maps old rows to `[]`, and must not infer I7-era behavior from retrieval policy snapshots.
- Benchmark `case_13_compaction_retains_negative_lesson` verifies metadata retention, zero positive contamination, zero unsafe leakage, and unchanged task success from existing positive/project context; it does not imply retained metadata entered the prompt. ✅
- Closeout includes affected regression, compile, full pytest, deterministic benchmark, reproduce script, docs/.ai sync, and unsafe-marker checks over generated report outputs. ✅
- Do not include Phase 4 async/lifecycle/governance or TS/MCP/IDE integration work in this I7 slice; those are tracked separately in `docs/design/PHASE4_PLATFORM_PLAN.md` and `docs/design/INTEGRATIONS_PLAN.md`.
- **I7.1-I7.6 status (2026-06-14):** complete. The retained-negative DTO/conversion, dedicated compaction-log Pydantic field, SQL ORM JSONB column, Alembic migration `0007_i7_retained_negative_evidence`, SQL serialization/deserialization, I7.3 packer metadata retention for dropped standard `avoided_attempts` blocks, I7.4 replay/metrics/reports/trace-bundle surfacing, I7.5 benchmark `case_13_compaction_retains_negative_lesson`, dashboard benchmark-summary parity, and I7.6 closeout verification/docs sync are implemented. Current benchmark is 13 cases × 6 strategies = 78 rows and `bash scripts/reproduce.sh` reports `acceptance.passed=true (13/13 checks true)`.

### Provider Registry + Key Ontology Requirements (completed plan)

- Add a unified `ProviderRegistry` with stable non-secret `ProviderCapabilities` snapshots for extraction, embedding, summarizer, and contract-only judge providers. ✅ P1 base registry/snapshot layer complete for provider-only infrastructure.
- Keep deterministic behavior as the default: deterministic hash embedding and rule summarizer remain the no-env path; real providers are config-gated and degrade at runtime call sites.
- Add deterministic and OpenAI-compatible embedding providers without changing the pgvector column dimension (`embedding_vector` remains 256-dimensional) and without removing `stable_embedding(...)` or repository fallback backfill. ✅ P2 provider classes complete; ✅ P4 runtime/retrieval embedding provider integration complete while repository fallback backfill remains unchanged.
- Build providers through `providers/factory.py`; keep `providers/registry.py` dependency-light and free of settings/provider-implementation imports. ✅ P3 complete for factory/DI.
- Inject the registry into `MemoryRuntime` while preserving existing explicit `extraction_provider=` and `summarizer_provider=` arguments for compatibility during this slice. ✅ P3/P4 front-half complete for registry injection.
- Bump retrieval policy snapshots to `retrieval-policy-v2`; include only retrieval-relevant non-secret provider snapshots (`embedding`, `summarizer`) and reflect explicit provider overrides. Exclude `judge` from retrieval policy hashes. ✅ P4 complete for observability/policy metadata and runtime/retrieval embedding hot paths.
- Add `app.memory.key_ontology` as the single source for canonical keys, aliases, cardinality, default `MemoryType`/`MemoryScope`, free-form validation, prompt rendering, and candidate normalization. ✅ P5 complete.
- Migrate `writer`, `resolver`, and `llm_extractor` away from duplicated key/cardinality/prompt rules and into ontology-derived behavior. ✅ P6/P7 complete for writer/resolver/runtime identity and LLM extraction boundary.
- Normalize LLM extraction aliases; drop unknown non-free-form keys; allow only explicit safe free-form keys; reject secret-like or wildcard concrete free-form keys. ✅ P7 complete.
- Force deterministic provider registry inside the benchmark runner even when real-provider environment variables are set. ✅ P8 complete.
- Use TDD per plan task and update `.ai/PROJECT_STATE.md` plus `docs/design/ROADMAP.md` §10/§11 after each completed task. ✅ P10 closeout complete.

### Provider Registry + Key Ontology Progress Completed (P1/P2/P9/P3/P4/P5/P6/P7/P8/P10)

- `app.providers.base` now defines `ProviderKind`, `ProviderCapabilities`, and an `EmbeddingProvider` protocol. Capability snapshots are deterministic, metadata is recursively frozen, and key/value secret-like metadata is removed before snapshotting.
- `app.providers.registry` now defines the dependency-light `ProviderRegistry`; `app.providers.factory` now owns settings-based construction and deterministic registry helpers.
- `app.providers.embedding` now defines `DeterministicHashEmbeddingProvider` and `OpenAIEmbeddingProvider`; `stable_embedding(...)` remains the deterministic primitive and pgvector dimension remains unchanged. `MemoryRuntime._prepare_embedding(...)` and `RetrievalController._embed_query(...)` use the embedding provider first and fall back to `stable_embedding(...)` on failure; `Repository.add_memory(...)` still performs deterministic `ensure_embedding(...)` backfill for direct seeded memories/tests/backfills.
- `app.providers.judge` now defines the contract-only `JudgeProvider` and `NoopJudgeProvider`; no evaluator/hot-path behavior changed.
- `app.memory.key_ontology` now defines canonical memory key specs, aliases (`project.pkg_manager` -> `project.package_manager`, `project.js_runtime` -> `project.runtime`), single/multi cardinality, default memory type/scope, safe free-form validation, and stable LLM prompt rendering.
- `writer`, `resolver`, and `MemoryRuntime` now use ontology constants/cardinality/canonical identity instead of duplicated raw-key semantics; historical alias rows participate in conflict/supersede matching with canonical incoming keys, package-manager facts (`npm/pnpm/yarn`, and correction paths such as `npm -> bun`) no longer overwrite runtime facts, and same-value alias survivors are promoted to canonical keys.
- `llm_extractor` now exposes `ExtractionCandidate.free_form`, renders its system prompt from ontology, canonicalizes alias keys in `build_results(...)`, drops unknown non-free-form keys, rejects unsafe free-form keys, and applies ontology default type/scope to controlled, wildcard, and arbitrary safe free-form specs.
- `benchmark.runner._run_case(...)` now injects `deterministic_provider_registry()` for every benchmark runtime, so real extraction/summarizer/embedding env vars cannot affect reproducibility; conformance asserts access policy provider snapshots are non-secret, contain only `embedding`/`summarizer`, and exclude `judge` from retrieval policy hashes.
- Current verified commands: P8 RED/GREEN completed; affected provider/benchmark/replay suite -> **70 passed**; strategy conformance provider snapshot suite -> **13 passed**; P10 closeout affected provider/ontology/runtime/retrieval/replay/benchmark/conformance suite -> **312 passed**. Final review hardening fixed settings-derived embedding providers to the fixed 256-dim pgvector contract, package-manager correction semantics (`npm -> bun`), ontology schema coverage, and summarizer provider factory wiring; final affected suite -> **322 passed**; compile `uv run --extra dev python -m compileall -q apps/api/app packages/python-sdk/src examples` -> passed; deterministic benchmark passed; `bash scripts/reproduce.sh` printed `acceptance.passed=true (12/12 checks true)`; full `uv run --extra dev pytest -q` -> **460 passed, 1 skipped**.

Phase 3-A scope: Retrieval Replay, eval tables, Quality/Safety profiler metrics, expanded profiler phases, dashboard-table extension, and static JSON/Markdown/HTML observability reports. Use `docs/design/P3A_IMPLEMENTATION_PLAN.md` as the concrete implementation plan before touching code. **Issues 1-8 are complete**: access fidelity + eval persistence schema, side-effect-free retrieval trace pipeline, replay service + deterministic diff semantics, replay/observability APIs, Quality/Safety metrics + profiler phase expansion, dashboard table extension, JSON/Markdown/HTML observability reports, and full regression/benchmark/project-memory sync.

**Context Compaction (ROADMAP §9):** Issue-by-Issue plan lives at `docs/design/CONTEXT_COMPACTION_PLAN.md`. **C0, C1, C2, C3, C4, C5, and C6/I7 are complete**: `PackResult` + all `pack_context` callsite migrations landed behavior-preservingly; over-budget ordinary block drops now emit `compacted_constraints` + `compaction_notice` with protected-block truncation; budget compaction now persists durable `ContextCompactionLog` records surfaced through observability/replay; C3 adds the deterministic-rule plus config-gated OpenAI-compatible `SummarizerProvider` seam with validation/fallback; C4 adds config-gated in-flight rolling active-history summaries that inject protected `history_summary` blocks and persist `ContextCompactionLog(kind=history_summary)` without rerunning summarizers during replay; C5 adds the retention-quality benchmark/report/replay loop (`case_9_over_budget_compaction`, compaction trigger/constraint retention/unsafe leakage/compression metrics, report Compaction section, replay drift coverage); and C6/I7 preserves dropped negative lessons as separate retained metadata without changing prompt semantics. Showcase assets (§12), Phase 3.5 SDK/LangGraph adapter/CLI (§6), 6-strategy benchmark expansion/eval-table persistence (§7), Security & Consistency Hardening (§13), Provider Registry / Controlled Memory Key Ontology (§10/§11), and I7 are already done.

**Context Compaction maintenance rule:** after completing each Issue in `docs/design/CONTEXT_COMPACTION_PLAN.md` §4, update `.ai/PROJECT_STATE.md` and tick or annotate the corresponding `docs/design/ROADMAP.md` / `docs/design/CONTEXT_COMPACTION_PLAN.md` checkbox/sub-checkbox.

### Phase 4 Async Foundation Progress (P4-A1/P4-A5)

- P4-A1/P4-A5, P4-B1/P4-B4, P4-C1/P4-C2, and P4-D1/P4-D4 are implemented. P4-A scope includes optional Celery/Redis dependencies/settings, JSON-safe task contracts, Celery app/queues/eager task wrapper, worker runtime factory independent of FastAPI `app_state`, in-memory/Redis idempotency stores, runtime-level `process_event_extraction(event_id)`, async `CandidateBufferProtocol`, deterministic in-memory buffer, Redis candidate buffer, API dependency selection for default in-memory vs opt-in Redis, complete extraction mode matrix, async enqueue fallback, opt-in dev compose, and optional Redis smoke coverage. P4-B scope includes lifecycle transition policy/audit, retention signal storage, access timestamp update, scheduler functions, maintenance task wrapper, and scheduler-backed `variant_3` reflection signal source. P4-C scope includes redacted memory versioning, semantic-change version creation, lifecycle-transition versioning, access-only version suppression, ontology-backed memory conflict scan, read-only conflict/version APIs, and dashboard table surfacing. P4-D scope includes default-off API key authentication and workspace authorization, prefix+digest API-key storage, resource-id workspace-owner lookup, fixed-window quota service, redaction state machine, and raw-payload retention guard.
- Durable requirements locked by review: async defaults must remain disabled/eager-safe with no import-time network connections; task payload/result contracts reject raw secret-like keys/values and non-finite numbers; production async idempotency must use Redis when async tasks are enabled; Redis buffer keys/set members encode workspace/session segments and store only post-redaction events; buffered flush and async worker extraction must re-check run/step/state terminal failure/rollback/cancellation before writing derived memories; event metadata is redacted before persistence/buffering.
- P4-A4 requirements: `ExtractionMode.async_ = "async"`, `sync_flush`, `lazy`, `no_extract`; `buffered` remains backward-compatible lazy behavior; `WriteEventResult.queued/task_id/warnings`; async task payloads carry only persisted event ids plus context ids, never full event content; enqueue success does not inline extract; enqueue failure returns `queued=False`, `buffered=True`, and warning `async enqueue failed; fell back to lazy buffer`.
- P4-A5 requirements: core `docker-compose.yml` remains PostgreSQL/pgvector-only; `docker-compose.dev.yml` adds opt-in Redis/worker services; docs use standalone `docker-compose`; real Redis smoke is gated by `MEMTRACE_TEST_REDIS_URL` and skipped by default.
- Latest P4-A verification after final P4-A4/P4-A5 review fixes: P4-A regression **67 passed, 1 skipped**; compileall passed; full `uv run --extra dev pytest -q` -> **526 passed, 2 skipped**; deterministic benchmark passed; `bash scripts/reproduce.sh` -> `acceptance.passed=true (13/13 checks true)`. Final fixes preserve request-level `buffered`/`lazy` natural-boundary flush semantics under a default `sync` runtime, keep request-level `sync_flush` explicit-only under lazy defaults, dedupe primary/local fallback drains by `event_id`, use Lua for atomic Redis append on real Redis, and restore Redis buffer discoverability after mid-drain failures.
- Latest integrations verification after cross-INT full review: INT-C review first locked exact config-template shape, clarified README cwd/placeholder semantics, removed stale roadmap wording, marked the umbrella Phase4/I7/integrations plan completed/superseded, and fixed ADR source attribution. Final cross-INT review then fixed broader MCP JSON secret-key redaction (`authorization`, bare `token`, `client_secret`, `secret_key`, `id_token`, `*_token`, `*_secret`, `*_credential`), switched the TS example to public `@memtrace/sdk` import with workspace path mapping, updated production-code boundaries, and marked stale INT-A/INT-B superpowers plans historical. Verification with temporary Bun: `npm exec --yes --package bun -- bun test packages/mcp-server/test/tools.test.ts` -> **16 passed**; `npm exec --yes --package bun -- bun run typecheck` passed; `npm exec --yes --package bun -- bun test` -> **27 passed, 1 skipped**; package-local MCP tests -> **18 passed**; SDK tests -> **9 passed, 1 skipped**; forbidden `node_modules` / `*.tsbuildinfo` / npm/pnpm/yarn lockfiles are not tracked. Earlier INT-A post-review hardening also verified `uv run --extra dev pytest packages/python-sdk/tests/test_http_backend.py packages/python-sdk/tests/test_backend_isomorphism.py -q` -> **13 passed**. Bun is not globally installed in this environment, so verification used temporary Bun via `npm exec --yes --package bun -- bun ...`; repository state uses `bun.lock` and no npm/pnpm/yarn lockfiles. Integrations must remain thin over `/v1` HTTP semantics and must not duplicate Python runtime/retrieval/gate/context-packing behavior.

## Coding Readiness

- **P0 production code:** Done and verified (45 pytest passing at P0; demo proves contamination elimination).
- **P1 production code:** Done and verified locally (50 pytest passing; benchmark report with §10.5 acceptance self-check generated; benchmark persistence and dashboard tables covered).

## P0 Outcome (Done)

The full P0 hot path from `mvp.md` is implemented:

- MemoryRuntime facade: `start_run`, `start_step`, `write_event`, `finish_step`, `rollback_branch`, `retrieve_context`, plus read models and `inspect_access`.
- Execution state tree (`root -> step/recovery`) with correct recovery placement and `failure_reason` preserved across rollback.
- Run-local monotonic `sequence_no`; every event bound to `step_id` + `state_node_id`.
- Rule-based memory writer (project +/- constraints, explicit correction supersede, tool_evidence, working_state) + secret redaction.
- Lexical retrieval, three-layer admission gate (hard/risk/soft), structured context packer, phase-aware profiler (retrieval/gate/context_packing).
- PostgreSQL source of truth via SQLAlchemy 2.0 async + Alembic; in-memory repo for tests; same `Repository` protocol for both.
- FastAPI endpoints for all `mvp.md` §3.1 routes + `/health`.
- Deterministic Bun-vs-Node demo emitting `reports/demo_report.{md,json}`.

P0 acceptance criteria in `mvp.md` §13 are satisfied; see `.ai/PROJECT_STATE.md` for the verification snapshot.

## P1 Outcome (Done Locally)

Per `mvp.md` §2.2 and §10, the P1 scope is now implemented:

- Active-path context builder and `active_path` context block.
- Generalized failed/rolled-back branch isolation through active-path filtering and `variant_2` gate rejection.
- The 4 required benchmark cases: project preference, failed branch isolation, workspace isolation, tool-call safety.
- `benchmark_report.md` and `benchmark_results.json` generated by `python -m app.benchmark.runner --output-dir reports` with metrics from `mvp.md` §10.2.
- Benchmark cases/results can be persisted through the repository into `benchmark_cases` and `benchmark_results` tables.
- Basic dashboard table API is exposed at `GET /v1/dashboard/tables`.
- P1 benchmark acceptance observed: `variant_2.failed_branch_contamination_rate=0.0`, `baseline_1.failed_branch_contamination_rate=0.25`, `cross_workspace_leakage_rate=0.0`, `tool_sensitive_blocked_rate=1.0`.
- pgvector semantic retrieval is restored (`pgvector/pgvector:pg16`): hybrid lexical + deterministic-vector cosine, `vector(256)` column with an HNSW index (migration `0002_pgvector`).

### P1 Non-Goals

- No LLM extraction pipeline, dedup/merge, conflict resolver (P2).
- No Neo4j, Elasticsearch, Celery, React dashboard, TypeScript SDK (deferred).

## Implemented Module Map

```text
apps/api/app/
  runtime/   models.py, repository.py, state_tree.py, memory_runtime.py
  memory/    writer.py, secrets.py, summarizer.py, summarizer_provider.py, resolver.py, candidate_buffer.py, llm_extractor.py
  retrieval/ similarity.py, gate.py, packer.py, profiler.py, controller.py, negative_evidence.py, policy.py
  benchmark/ cases.py, evaluator.py, runner.py
  observability/ metrics.py, replay.py, reports.py
  storage/   orm.py, db.py, sql_repository.py
  api/       deps.py, routes.py
  demo/      run_demo.py
  config.py, main.py
apps/api/tests/   runtime/, memory/, retrieval/, benchmark/, api/, observability/, storage/, conformance/ (full regression count changes as features land; see `.ai/PROJECT_STATE.md` latest verification)
migrations/       env.py, versions/0001_initial.py, versions/0002_pgvector.py, versions/0003_memory_superseded_by.py, versions/0004_phase3a_observability.py, versions/0005_context_compaction.py, versions/0006_security_consistency_hardening.py
packages/python-sdk/   src/memtrace_sdk/ (SDK facade/backends/CLI/LangGraph adapter) and tests/
```

## Standing Requirements From Design Docs

- Do not build a generic knowledge-base/RAG app as the main product.
- Treat memory as an Agent runtime component with trace, state, retrieval, gate, and profiler.
- Persist raw trace before derived memory extraction.
- Exclude failed/rolled-back/superseded branch memory from prompt context by default.
- Keep PostgreSQL as source of truth.
- Keep deterministic and demo-oriented through P1; defer LLM extraction, Neo4j, Celery, and full dashboard.
- Benchmark fairness: vector-only and gated variants must use identical seeded memory items.
